/**
 * VideoScene.tsx - V16 Constraint-based Combinatorial Editorial Optimizer
 *
 * 系统形式化：
 *   π* = argmax_{π ∈ 𝒫} F(π)
 *   其中 π = transition sequence (whip/fade/zoom)
 *   𝒫 = 满足 budget + cooldown + diversity 约束的合法路径集合
 *   F(π) = evaluateFullSequence(π) — 全局能量函数
 *
 * v16 三层优化结构：
 *   Layer 1 (Decision)   → decideTransition()     — constrained action space
 *   Layer 2 (Search)      → beamSearchTransitionPlan — beam search over discrete space
 *   Layer 3 (Eval)       → evaluateFullSequence()  — continuous global energy function
 *
 * v15 vs v16 本质区别：
 *   v15: score = Σ Δs_t（Markov 贪婪累加，beam search 保留多条 greedy）
 *   v16: F(π) = GlobalStructure(energy, entropy, pacing, semantics)
 *         rolloutEstimate ≈ E[F(π_full)]（有限视野近似，接近 MCTS rollout）
 *
 * 架构定位：
 *   从 "剪辑逻辑系统" → "Sequence-level optimization engine"
 *
 *   v12: reactive, frame-level, module-state
 *   v13: planned, shot-level, pure function
 *   v14: greedy, timeline-level, constraint-aware
 *   v15: beam search, globally-aware scoring (伪全局)
 *   v16: full-sequence scoring + Monte Carlo rollout ≈ Deterministic MCTS
 */
import React, { useMemo } from "react";
import { AbsoluteFill, useCurrentFrame, useVideoConfig, interpolate, Easing, Img, spring } from "remotion";
import { FONT_FAMILY } from "./constants";
import type { VideoLayout, VideoElement, TextElement, ImageElement, StickerElement, BackgroundElement, ShapeElement, Shot } from "./types";
import { evaluateDirector, type DirectorState } from "./directorEval";
import { buildFeatureVector } from "./rewardModel";

// v20 MLP inference (duplicated here to avoid circular dependency with rewardModel.ts)
// Weights from dataset/reward_model.npz (trained on v19.6b data, spearman=0.66)
// t=28, h1=64(relu), h2=32(relu), out=1
const _w0: number[][] = [
  [0.2261330932,-0.0593081117,0.2158851773,0.4061208367,-0.1449858695,-0.0607567355,0.4069519639,0.2597843707,-0.1553051919,0.1982983053,-0.1354273558,-0.1275489032,-0.0250805132,-0.5179922581,-0.3870543838,-0.2069543302,-0.2878653407,0.0839861333,-0.2445215732,-0.4272767603,0.3936770558,-0.1363457143,0.0177190453,-0.3843366206,-0.4080383480,0.1623001397,-0.3204410672,0.1012848839,-0.1710585207,-0.1576975882,-0.1610403955,0.5248732567,-0.0072800885,-0.3248607814,0.2090284675,-0.5421559811,0.1122593731,-0.5829672813,-0.3749442995,0.0545122735,0.1969877332,0.0324715264,-0.0270479731,-0.1647461355,-0.5065694451,-0.1942627877,-0.1316615194,0.2758004963,0.2084810138,-0.4949086905,0.1959623694,-0.1052150279,-0.1379925460,0.1456881762,0.0941237658,0.3253914416,-0.2249899209,-0.0865600258,0.0712611303,0.1973587722,-0.0974947810,-0.1169253439,-0.3332485557,-0.3461240828],
  [0.2064602673,0.2684928179,-0.0004221087,0.2653844059,-0.0566669218,-0.1864521652,0.0574078150,0.4310498834,0.0144588156,0.4336915910,-0.7441949844,0.2005643696,-0.1309781522,-0.0793952644,0.0208698269,-0.6663011312,-0.0449449420,0.0954423472,0.4005841613,-0.2630245984,-0.2063041329,-0.2411208898,0.2353315055,0.0820036680,-0.4729087949,0.1592735052,0.0306974500,0.2598299980,-0.1684862077,-0.3271093965,-0.1050155684,-0.2586997151,0.0878565609,0.0776174664,-0.0283133592,-0.3327814937,-0.2929169238,-0.2596846521,-0.0804889947,-0.2005058080,-0.0496648028,0.0936680436,0.5142398477,0.0434584543,-0.1614756137,-0.0433133207,-0.3982384801,-0.0006732591,0.1399305612,0.6665214300,0.0225366950,0.0908530354,-0.0151456920,-0.3954870105,0.0723538175,0.1926872432,0.2128994763,-0.2489896417,0.3761146367,-0.4915961027,0.1279179603,0.5849082470,-0.3387242258,-0.1493591964],
  [0.0157084931,-0.1619942486,-0.4658698440,0.0173757803,-0.3001619577,0.1281510144,-0.2449583262,0.3968142569,-0.2047163099,-0.0892606825,0.2230330855,-0.3209589422,0.0014930530,0.3460828066,-0.4087889791,-0.0423006937,0.0784167647,0.2089509517,-0.3288428187,-0.4000502229,0.1377659291,0.0151214395,0.0758114159,0.1000130922,-0.2793045044,0.0565001927,0.0685331523,-0.2063901871,0.5186139941,0.0158883128,-0.3183587193,0.1924951971,-0.2548295557,0.1766783893,0.3207135499,-0.3115045428,0.2815065086,0.0490986481,0.2156396210,0.5071698427,-0.0655721277,-0.1912568361,-0.2501753569,-0.2285821289,-0.1090514064,0.0797806606,0.1151511967,0.2003601193,0.0492824577,0.3657195568,-0.1198945269,0.7298778296,0.1904744506,-0.2412690818,-0.3710610867,0.0984297618,-0.0594075285,0.2057939768,0.1413048506,-0.0928737372,-0.2196365595,-0.4173711240,-0.1601081342,0.2230153978],
  [0.0465251654,-0.3597933054,-0.0040671173,0.1020519733,-0.2521269619,0.0426295325,0.0163088012,-0.3225269318,0.1001420617,0.1467571259,0.2949508429,0.2894749045,-0.4262436628,-0.2538411319,0.1580382586,0.0476039052,0.1464232653,1.0296858549,0.1542861760,0.2573449314,0.2532753050,0.1111971140,-0.0755820349,0.2101073116,-0.3020466864,-0.0687470287,-0.1393058449,0.0067366920,0.6381621361,-0.6074581146,0.1834406108,-0.4143521190,-0.1205836385,0.2580666542,0.0280117635,-0.3782614470,-0.1676469594,0.1217010543,-0.1991780251,0.0580765046,0.0121899415,-0.1641751230,0.5608131289,0.1590970457,-0.6278198361,0.0386766009,-0.1365373433,0.2075459063,-0.1669697016,-0.0529378913,0.0868394896,0.2342039198,-0.2980259955,-0.1013260707,-0.2099959254,-0.2044816762,0.4721460938,0.1228891909,-0.3224717081,0.1734494716,0.5737078190,0.2636909187,-0.4459798634,-0.1351599246],
  [0.4011735320,-0.2606412172,0.1761242598,0.2126727551,-0.3346217573,-0.0116615845,-0.8740323782,-0.2698389292,-0.0824923813,-0.3378685415,0.3441463411,-0.3920511007,-0.1927016526,0.0274719298,0.4711990654,-0.5215295553,0.2702161968,0.0027349007,-0.2577922046,-0.0075273658,0.0618173070,-0.2474974096,0.0318117216,-0.1201724187,-0.3247514963,0.2596648633,0.4143525064,-0.3389639258,0.5180352330,-0.6197320819,-0.0413232744,0.2630528510,0.0702659413,-0.1839424521,-0.0780761167,-0.3952327073,-0.0813992172,0.0470986515,0.0800895616,-0.1725254506,0.2299592346,0.0620832257,0.2231275290,0.1052046865,-0.3576951623,-0.1627157032,0.3137572706,0.1696593761,0.1539240479,0.0206001289,0.4970468879,-0.1537784785,0.1853150129,-0.0989031270,-0.3091982305,0.3521270156,0.2178004980,0.1781634390,0.3279896677,-0.0782760903,0.2372870594,-0.1038561016,-0.0251939129,-0.0801196247],
];
const _b0: number[] = [0.0008739436,0.0016767206,0.0034718493,0.0001114158,0.0006951320,-0.0001057860,-0.0001161604,0.0012444963,-0.0002979910,0.0004212385,-0.0003542848,-0.0005764537,0.0035791870,0.0002435223,-0.0013583878,0.0059295422,-0.0005544023,0.0000000000,-0.0001209854,0.0027059026,0.0001382255,0.0038469543,-0.0005486440,-0.0004777297,0.0056262966,0.0005766602,0.0006232067,0.0010015566,-0.0012604970,0.0066419421,-0.0000019263,-0.0008817064,-0.0003434454,0.0021546213,-0.0007857346,0.0053837756,-0.0014348816,0.0035947480,0.0002836272,-0.0000137216,0.0000135385,-0.0007099203,0.0008001343,0.0006003361,0.0052123144,0.0007406961,-0.0026013399,0.0013714818,-0.0027132230,0.0014458883,0.0034762411,-0.0001787253,-0.0014952369,0.0005967427,0.0050466410,0.0021177549,0.0000134331,-0.0009632227,-0.0009726390,0.0044842148,-0.0004616238,0.0006517191,0.0026433878,0.0003921925];
const _w1: number[][] = [
  [-0.0399602465,0.1186772808,0.0410690457,-0.1959320158,-0.0213452056,-0.1373709291,0.2451136559,-0.0591110438,0.3355530500,-0.0178525932,-0.1655636579,-0.2385764867,-0.2713915706,-0.3417526484,-0.0230529252,0.0821886957,-0.3389828801,0.1729687303,-0.1144848093,0.0161080807,0.2589812279,0.1106723100,-0.0488646887,-0.1574997157,-0.3967013061,-0.1301906854,-0.0373229049,-0.1582482010,-0.0201959945,0.0610247105,-0.0973786712,-0.1149053052],
  [0.0248358771,0.0733395815,-0.1924122125,0.2992241383,-0.0497529134,0.0021260509,0.1602071673,0.0700926110,0.1687679887,0.0329786912,-0.0871762335,0.0677708015,-0.1873916686,0.0931808576,0.0257670358,-0.0302225817,0.0221848935,-0.3422966301,-0.0759902596,0.0224882979,0.0441543125,-0.1514405757,0.2256574035,-0.1400478482,0.1879259348,0.0668880492,-0.1142379269,0.2895194590,-0.2124875933,0.1810363084,0.1771800220,-0.1175034940],
  [0.2319078594,-0.0581950322,0.0748250932,0.0394048356,0.0231497604,0.1437851638,0.1809049994,-0.3364821970,0.2308384180,-0.2879799008,0.1611035168,0.2223271579,-0.1821184456,-0.0928016454,-0.2210666537,-0.0445208587,-0.0907018110,0.1497611552,0.2330438793,0.2417476326,-0.1228841096,-0.0811382905,-0.2980499864,-0.1428335756,0.1630335599,0.2721150517,-0.2182041109,-0.0991192013,-0.0039418996,0.0606553145,-0.1763169318,0.0783626586],
  [-0.0854576156,-0.0036391665,0.0984123275,-0.0229087994,-0.0702710599,-0.0178705882,0.0007589467,-0.0850225538,-0.0458476581,0.2780013084,0.0861722156,-0.0430796668,0.1355924159,-0.0753552690,0.1856893897,0.0626891777,-0.0391308814,0.0843846500,-0.2873107195,-0.2025501728,-0.2205643207,0.0089927251,0.3053798676,0.3941460550,0.1063091382,0.0855963603,-0.3183850348,-0.0824950337,-0.1390335411,-0.0973657966,-0.0471375026,-0.0831951201],
  [-0.3540859520,0.1317497790,-0.1885593235,0.1773086637,0.3141603470,-0.1653178185,-0.4336575866,0.0440896079,0.1967741400,0.3372399807,-0.1523877382,0.0671816021,-0.0951916203,0.1284636110,0.5718849301,-0.2514961958,0.1483949870,0.0576217249,0.2646722794,0.0350390337,0.1007607505,0.0158950482,0.0523741208,-0.0389535651,0.1479734182,-0.0694324747,0.0666625649,-0.0388550200,0.1943240613,-0.0074321427,-0.2587435246,0.2231681794],
  [-0.1628009379,-0.1189393103,-0.0956116021,0.2572737634,-0.3683993518,0.5490823984,0.1939960420,0.0401434191,-0.0083492799,0.0584955104,0.0917175040,0.1197806969,0.0952639133,0.0176685657,-0.0323428102,-0.0283840094,-0.0427762084,0.2019015402,0.1143527552,-0.5197242498,0.1117054746,0.0274984892,-0.0033072722,-0.0684049129,0.1977293193,0.1634766757,-0.1366267651,0.0719573572,-0.1717662662,-0.2438843399,-0.1134216189,0.1533260345],
  [0.1705551296,0.0930695906,0.1281191111,0.1445728540,-0.1335807741,0.0618684627,0.1449212581,0.1655672342,0.2130931318,0.1238995492,0.0860595629,0.1774190515,-0.0183966719,-0.0925945714,0.0070201629,-0.1262275875,-0.0909860507,-0.0360418670,-0.0957573801,-0.0478298515,-0.3173579276,0.0721504688,0.0012855335,-0.1350785047,0.1289662719,0.1635783911,-0.2841791213,-0.1155995503,-0.1401804090,-0.1674596369,0.1878892332,-0.0347083732],
  [-0.0483193137,-0.3561448753,0.1123270988,-0.2502592802,0.0072639161,0.0490337797,0.2710622251,-0.2594609857,-0.5338414311,0.0201135073,0.3012634516,0.2337039709,0.0598350689,-0.0976155922,0.1115342677,-0.1742565632,0.0638504699,0.0948624909,0.1719836593,0.4837703109,0.0588092096,-0.1078628078,-0.0057699694,-0.3110978901,-0.1297423840,-0.0779943168,-0.0926466137,0.0399078280,-0.1452738941,0.2202601433,-0.0263362266,-0.0012050495],
  [-0.0596562512,0.0274340957,0.1461770833,-0.0905564129,-0.1436717808,-0.0534731857,-0.2375654876,-0.1005518734,-0.0832915232,0.1528209150,0.0769741014,0.0232519172,0.1411961764,0.0395599827,0.1179323569,-0.0115699871,0.0966780782,-0.1149705797,-0.4013209343,-0.0153181795,0.2735832930,0.2032906562,0.1728077233,0.2268092483,0.0334396474,0.0341410376,-0.1091864631,-0.0801113099,0.1071450636,0.2089047730,-0.0006069162,-0.0795829669],
  [-0.0043388326,0.1008171886,-0.2547969222,-0.4329217374,-0.0846830681,-0.2231085896,-0.0648353249,-0.0566887185,-0.0797139481,0.2448850721,0.0387063101,0.0669994131,0.1205356568,-0.2108290941,0.1497141272,-0.0150529947,0.0964984596,-0.1865272224,-0.2840391397,0.1397358030,0.0536309630,-0.1811027676,-0.1546132714,0.1995150596,-0.2155492008,0.2825684249,-0.1592087597,0.1240928844,-0.0580920875,0.1184817925,-0.1138192266,-0.0275427736],
  [-0.0088705188,-0.1804407537,0.1394234449,-0.0313269012,-0.0061673396,-0.1695413291,-0.4384567440,-0.0002838328,0.4276205301,0.1386997551,0.0170417968,-0.0552297980,-0.0087328264,0.1442451179,-0.1712264419,0.2002500445,0.1537811756,0.1201761141,0.1246009916,0.0086804330,0.1303180903,0.1262084097,-0.0595275275,-0.0915019736,0.0344427861,0.2706573009,-0.2190452516,-0.2578954697,0.0268236846,0.0104246326,0.0533830822,0.0438657850],
  [-0.0427917056,0.2592003047,-0.2804750800,-0.1233748719,-0.0246463101,-0.2990260422,-0.0191743523,0.2980461717,0.0039787018,0.0384106934,-0.2188862711,-0.1053858027,-0.0635802299,-0.0322434790,0.2324603945,0.1005720645,-0.1861044765,0.0405858122,0.0984528810,0.2031547874,0.2822954059,-0.1750818193,-0.1425929517,-0.2190342247,-0.0353177451,0.0780872628,0.1745377332,-0.0315011218,-0.0566103868,0.0284756534,-0.1451101750,0.3703685105],
  [-0.2841970026,0.0326578282,0.3577286899,0.0425802730,-0.0399984270,-0.0642429888,-0.0342600532,0.2757530808,-0.3904873431,0.2696562111,-0.2308783829,-0.0546179898,-0.0836711451,0.1475489587,-0.2460161000,0.0854888782,0.3874883950,-0.2475101799,0.0590191707,-0.1120998189,0.0881707743,0.0292865448,-0.1118094325,0.0927861854,0.3295284510,-0.0346008986,0.1272364259,-0.2294160128,-0.1707884967,0.0805542693,0.2845657766,0.0628641546],
  [-0.0553413630,-0.0001252630,-0.2210429460,0.1424788386,0.1334130466,-0.0836448073,-0.0830924958,0.0701868832,-0.0758751258,-0.0116907051,0.3878522217,-0.0457791276,-0.0673423707,-0.0813312605,0.1563718021,-0.0282532889,-0.0883437619,0.2252799869,0.2623101771,-0.1018670425,-0.3187490106,0.2004149407,-0.1199373975,0.1440565288,-0.3396488428,0.1567391455,-0.2141069770,0.1053263843,0.0024138454,-0.1693373024,-0.0654129460,0.1213251054],
  [-0.0008749682,0.0925870314,0.3643814325,-0.2643339932,0.3143693209,0.0289239008,-0.1103543043,-0.2460812479,-0.0344128534,0.1580446362,-0.0314157754,0.2868610024,-0.2307755500,-0.0001787947,-0.2322479039,0.0620943271,0.1497769803,-0.0693154484,-0.4614293277,0.0331678428,0.0659613460,0.0504267067,0.2197770625,-0.1843331009,0.1226763874,0.2005426139,-0.3144128323,0.0236981250,-0.0867738053,0.0098520434,0.0175790340,-0.0836081207],
  [-0.1195170879,0.0060373517,-0.1920801848,0.0288030803,0.0660565794,-0.2044298500,0.0742977113,0.2555200458,-0.3009810448,-0.1197776422,0.3259387612,-0.2319759279,-0.1588388085,0.3065229356,0.3310514688,-0.0212941114,0.0674949884,0.2072675824,0.4371128380,0.2026907653,0.0927536115,0.0008518358,-0.1390209347,0.1182011515,0.1353487819,0.2477699667,0.1149844155,-0.2943194807,0.1761675477,-0.2265311778,0.0689063147,0.0442295671],
  [0.3475570679,0.5490706563,0.1093100384,-0.0377110876,0.1059647053,0.1583566219,-0.0449268147,0.3076625764,0.0404151529,0.0879893750,0.1109165251,0.0806505308,0.1577782929,-0.1199848950,-0.1096114218,-0.1960292161,0.0668891594,0.1405133158,0.0787216201,0.2942383885,-0.0059984699,0.1091858372,-0.1928113252,-0.0678562969,0.1213461161,0.1494168639,-0.0519665070,-0.0163620785,-0.2674072087,-0.0536990613,0.1447893083,0.1018303707],
  [0.0885060281,0.0087988283,0.0012486018,-0.1167293862,0.1235365793,0.0744182095,0.0869774669,-0.0929907039,-0.3806608319,0.1939510256,-0.0846473053,-0.1525186449,0.1225909069,-0.0692986995,0.1873720586,0.1090722755,0.1208391264,-0.2414691299,0.2142434567,0.0461830050,-0.0652795956,0.0253477395,-0.3139970005,0.0722402930,-0.1819688976,-0.2391205877,-0.2691176236,0.1966973692,-0.1112391055,0.2711273134,-0.0947172120,-0.3018210828],
  [-0.2099669874,0.2029548734,-0.0131118670,-0.0998725593,0.0673478246,0.0824446753,0.2278480977,0.2323140353,-0.0634909496,-0.2393830270,-0.0908221975,0.0630437136,-0.1703701168,-0.1692019701,0.0377757102,-0.0114341537,-0.0151664205,-0.1293430030,-0.0358642042,-0.1675364077,0.1395873278,0.1498453021,-0.3263629973,-0.1643158644,-0.2660354376,-0.1295045018,-0.0147498883,-0.2562634945,-0.1629633307,-0.1774762720,0.0202441979,0.0159134660],
  [-0.1363747865,0.0312578082,-0.0942432284,0.0854272991,0.2668288648,0.2381767482,-0.2257626802,0.2386598289,0.1446971446,-0.2029861957,0.2059475780,0.2509937286,-0.2122104466,-0.0392567441,0.2812954187,0.0603584237,0.1926007420,0.0379563682,0.1375429481,-0.2184064239,0.2269928306,0.2835782170,-0.0351162329,0.0110038891,-0.2344067693,0.0909082592,0.1011264250,-0.0589888059,0.1960361600,-0.0617331676,0.1482968479,-0.1802528948],
  [-0.0037918310,-0.0304166339,0.0452257171,-0.0536656007,-0.0236535259,0.1432310641,-0.0773271695,-0.2793657780,0.3106345534,0.2523652911,-0.2256713808,0.1238534525,-0.0918967277,0.0472401716,0.0577172004,-0.0424305946,-0.1410492510,-0.1217102036,-0.1558413655,-0.1460639685,-0.0117540704,-0.1263404042,0.1205709130,0.2629115283,-0.1031238809,0.0416222513,0.0883326828,0.0834390000,0.0134271216,0.1312889457,0.0863199085,-0.2187995911],
  [0.1487371624,0.1568523347,-0.1333812624,0.1389673203,0.0859528780,-0.0359412581,0.1918788701,0.5145820975,-0.1366952658,-0.2485265136,0.1186638027,0.1030132994,-0.0642154589,0.3940183818,-0.1297590584,0.1359674186,-0.0057984786,0.2794927061,0.4275658727,-0.1182251498,-0.0519104339,0.2348926961,-0.0634204969,0.0120824659,0.0417460352,-0.3410726786,-0.0024461909,-0.1465205401,-0.0921811610,0.2484922111,-0.1587060988,0.1437557191],
  [-0.0513819419,-0.0494550765,0.1402754188,0.0553181283,0.0927561373,0.1716006398,-0.0371702723,-0.1167294905,-0.1919519454,-0.1512150019,0.5168229342,0.2284990549,-0.2207434624,-0.2323691845,0.0755070522,0.0962717235,0.0927254558,-0.0432330929,-0.0277034845,0.0571510233,0.0130682429,-0.3957820535,-0.0407978073,-0.1478117555,0.0298794471,0.5254045725,0.0649621934,-0.0597838089,0.1635753959,0.1029796675,0.0567962378,0.1072770432],
  [0.3598172665,-0.2045121640,0.0878284574,-0.1205737740,0.0804455504,-0.1653141975,-0.0648004636,-0.0290083028,0.1725270450,0.1605625451,-0.2046547681,0.0096884277,-0.1254371852,-0.1806323379,-0.0118349260,-0.0507091582,0.4575312138,0.0704237968,-0.2280435115,-0.0953062996,0.1906945556,0.3865235448,-0.2788611650,-0.3270636201,0.3422686160,0.0645582452,-0.1534939259,0.0618135743,-0.4660691619,-0.0005397843,0.0630249679,0.1644560546],
  [-0.1944421381,0.0151474932,-0.1597664803,0.2870069146,-0.2069547623,-0.0705788732,-0.1553607881,0.2843123376,0.1548848599,-0.0499863587,0.1194149032,-0.2934320271,-0.0773739964,0.2325323522,0.6500829458,-0.1736876518,0.4317783117,0.1356420517,0.1863399297,0.3403279185,0.1425404400,0.2124067843,0.0002162837,0.2546506822,0.2602278888,-0.1141449884,-0.2332121581,-0.0894849971,-0.1486878991,-0.0073226197,0.3145155013,-0.0808402747],
  [-0.0154245505,0.0102570672,0.1523923129,-0.4325486124,-0.0898914412,0.0390023552,0.2511831224,-0.1249350831,-0.3320357203,0.1045038477,-0.1360128969,-0.1915103495,-0.0769494399,-0.0671350807,-0.1493226737,-0.3941691518,0.1235932261,0.2337106615,0.0969573185,-0.2069253474,-0.0509808399,-0.0286963172,0.1050476879,0.1193947271,-0.1392782927,0.0124950213,0.0808365196,0.2628051937,0.1265622675,0.1727668196,-0.0243946481,0.2493363023],
  [0.0629788935,-0.1522202790,0.2505433857,0.1214788482,-0.0638758615,-0.0830854401,0.0766998380,0.0689573139,0.0343224034,0.0550321192,0.2921103537,0.1997670680,0.0557057187,0.1531180143,-0.1972190589,0.0685974658,0.3068962693,-0.0350454859,-0.1522170901,-0.1174056903,-0.1984576285,-0.1492005885,-0.0648274496,-0.2458333522,0.0017009070,0.1595015824,-0.1604701430,0.2537518740,0.0923254564,0.2033822536,-0.1392838657,0.1510601491],
  [-0.1930284649,0.1573750079,0.0303771906,0.1267046034,-0.2358616590,-0.1592922807,0.1248692870,-0.1488021016,0.0827151835,-0.2503046393,0.1642676592,-0.2168046385,0.1064620465,0.1542396843,-0.1025192887,0.1065958813,0.0706234500,-0.1006656215,-0.1649941653,-0.3428897560,0.0726162344,-0.2551143765,0.0175238457,-0.0386110544,0.1281790137,-0.0572629683,-0.0582386926,0.1228721440,0.0578685254,0.0689870194,0.0847803056,-0.2714533508],
  [0.1291599274,0.1082702130,-0.1788899899,-0.0549384579,0.0066761668,-0.0223605279,0.0692641512,0.2387024015,0.1909707189,-0.2319697738,0.1003472209,0.2413294762,0.0779476091,0.1761536896,0.3396546841,0.1788764000,0.0304041374,0.1886104047,0.0210120827,0.0020058954,-0.0680763051,0.2642504871,-0.1446791142,0.2704610825,0.0856629238,-0.2473671436,0.0649918243,-0.3712357581,0.1114989370,0.1656301022,-0.1167005152,0.0218403526],
  [0.3447281718,-0.0904588327,-0.1790664792,0.3770774305,0.3352682292,-0.0151699055,0.1658168435,0.3129392266,-0.1298815012,-0.0726594105,0.2651021481,0.2787139714,0.0753045008,-0.0597980581,0.4382156730,-0.0866394937,-0.0014057779,0.0470923595,0.3480938673,0.1633794904,0.1023022532,0.1357747316,0.0114657329,0.1110735461,-0.4220582247,-0.0532013141,0.0426600948,-0.2143468261,0.0645843670,-0.0530127995,0.3745691478,0.2783630788],
  [-0.0823435709,-0.0266645923,-0.0130722066,-0.0780423433,0.0344758704,-0.1357776672,-0.1998664439,0.1106686741,0.1118583381,-0.1421842873,0.1589682698,-0.1110754162,0.0430672802,0.1465246975,-0.0056095654,0.0806062743,-0.0912927240,-0.0358467847,-0.0354656801,-0.1319776475,-0.0312495530,-0.2724568844,-0.0868245363,-0.0501030572,-0.0452866107,-0.0431448892,-0.0109184710,0.0847540870,0.1545942426,-0.1148633510,-0.2126978636,-0.1842091680],
  [-0.0937225297,-0.0622161627,-0.1258549690,-0.2110989690,-0.0617037602,0.0350503959,0.1209042221,-0.0951834843,-0.0713478103,-0.1553861052,-0.0034428434,-0.1476330459,0.1014148742,-0.3125544488,-0.0584866926,0.0517072231,0.0270195007,0.4778095484,0.0809038430,-0.1761288196,-0.0170878917,-0.3165098131,-0.0136277620,-0.1442081630,0.1637047529,-0.0980691984,-0.0206414927,-0.1030505896,0.3066496849,-0.0481820107,0.3261098266,0.1440562755],
  [0.0739790946,0.0513624810,0.0889435038,-0.3337089717,-0.0144804986,0.1485340446,0.0183490571,-0.2605209947,-0.0091891028,0.3039445877,0.2090087235,0.0041949814,0.1681940556,-0.0647898987,-0.3106360734,-0.1671905369,0.2579330206,-0.0415870547,-0.0352122225,-0.0555952899,0.3210130036,0.2582414746,0.3922286332,-0.0281154867,0.0503212772,-0.2811711133,0.2610875964,-0.2064362168,0.0383208692,-0.1939775199,-0.1222981438,-0.1447061449],
  [-0.1078258157,-0.0957552120,-0.0969211459,0.1625958085,-0.2012981921,0.0367692150,0.2161581516,-0.0803591534,-0.1263359338,-0.0417232253,0.2156220675,0.0342967249,-0.0958183184,0.1007520407,-0.1837088317,0.1220793650,0.0814152285,-0.0574075468,-0.1413154751,-0.1552394927,0.0984977633,0.2265113443,-0.1121583506,0.0832256153,-0.3654251099,-0.1042509302,-0.0054904954,-0.1694156379,-0.1656057537,-0.1183574572,0.0552545935,-0.0330133140],
  [-0.3955304325,-0.3743478656,-0.1072893664,0.0813902467,-0.4869923592,-0.0899526253,-0.0874126405,0.2450347990,-0.0672255158,0.0651601925,0.0252712946,-0.3760152459,0.1358178407,0.0448211282,0.0891808346,0.6941881180,-0.3691263199,0.3039578497,-0.0456756242,0.0510289744,-0.0074154004,-0.0729999021,-0.1008665487,0.0597993843,-0.2679170370,0.1311598718,-0.0735734701,-0.1880053133,-0.0804151595,0.2262678295,-0.0916937366,0.0632478148],
  [-0.1331150979,0.1339829266,0.1222698614,0.5341934562,-0.0927222371,0.1149058267,0.0060219741,0.3822723627,-0.1239531487,0.1301870197,0.0672595873,-0.0478319712,-0.0211886596,0.2483087033,0.1481550932,0.0584748536,0.4346451759,0.1366524696,0.2602113485,-0.0544305630,-0.0889534503,0.2972531319,0.1625210196,0.2881026566,-0.0514421910,-0.0422110297,-0.2898225784,-0.1298061758,0.1704054475,-0.1005150005,0.2034155428,-0.0336765088],
  [0.0513968244,-0.2028850913,0.0166295506,-0.4260212183,-0.0522366352,-0.2871600688,0.2081039101,0.2105949223,0.0857008174,0.2657192349,-0.1167879999,0.2822841108,0.1289300770,0.0661350191,0.2815019190,-0.1842691600,-0.0661659911,-0.1306867301,-0.0274145100,-0.1726324558,0.2531863451,0.0204453859,0.0659833848,-0.0868366510,-0.2909718752,0.0464414582,-0.3380421102,-0.0184986703,-0.2472568303,0.3357826769,0.2238585204,0.0186948050],
  [0.2113201320,-0.1496089399,0.0992415398,0.1911510527,0.0124221314,0.2270863205,0.1408926845,0.1555334181,-0.2915237546,-0.1697138101,0.0478153415,-0.0044489102,-0.2076705545,0.5668745637,0.3831651509,0.1204578280,0.1220882386,-0.0621162318,-0.0913771316,0.0320972204,0.0934027955,-0.1323033571,0.0655739978,-0.0050701909,-0.2950526178,-0.1520960182,-0.0731158182,0.2693032622,0.0960165262,-0.2328744084,0.1340835840,-0.2735272944],
  [0.1858786792,-0.0832838565,-0.0151920589,0.3075431883,-0.2509341240,-0.2491076440,-0.0977318361,0.2297340631,-0.2667737603,-0.5006591082,-0.0474636219,0.0897713304,0.2049201876,-0.0159900561,-0.0226361975,0.2110397667,-0.2791742682,-0.0829397142,0.0669327304,0.0880794749,-0.0675402507,0.0481258668,-0.4370693564,-0.1063549295,0.0997427627,-0.1746150702,0.0104270354,0.0427493639,-0.0614726506,0.0310139470,-0.1505184621,-0.0365240574],
  [0.1091383696,0.0291882940,-0.0998016894,0.0531738102,0.1236019656,-0.3684530556,0.2032193094,-0.0557806715,0.0758180916,0.2212877572,-0.2398928553,-0.1257026643,-0.0512140393,-0.1383690536,0.3123883605,-0.1522132903,-0.1021113023,0.0369873531,-0.2037781179,-0.0688710660,0.0569619611,-0.0582285859,-0.0165086202,-0.0935735852,-0.2681258321,0.0583296232,0.3102312684,0.0032529794,0.0397994518,0.1222980395,-0.2182835788,0.3007423282],
  [0.0291212592,0.2803362906,-0.1159699783,0.3279477954,0.1573060453,0.1010578945,0.0569920093,-0.0465838946,0.2121441662,0.0282836966,-0.3522523046,-0.1888496727,-0.0328531973,0.0548005961,-0.0104198046,0.2128381878,-0.3538942635,0.0304740779,-0.3169256449,0.1332440376,-0.0255780425,0.0576841086,-0.1527143717,0.2261795849,0.1827783883,0.0954121649,-0.0228956304,0.1025052518,-0.1249621287,0.1512423009,0.2779814899,0.1912016869],
  [-0.1328886002,0.0594118573,-0.2244572639,0.0955015942,0.0913060680,-0.3302763104,-0.1950625628,-0.0582521670,-0.0526215807,-0.2171757072,0.0626618788,0.0514175817,-0.0348646790,-0.1353133172,0.1762239188,0.2915200591,-0.0919978395,-0.1070405543,0.0848836526,-0.0648590699,-0.0305739474,0.1206362695,-0.3376346231,0.2330170870,0.0116645312,-0.0781811848,-0.0157745183,-0.0048356103,-0.3059881330,0.2660872340,-0.0022657663,0.0793302283],
  [0.1614864320,-0.1887301654,0.0882430151,-0.0599626899,0.1697380692,-0.3719248176,-0.0021767525,-0.1203162745,0.0763328448,0.0746564120,-0.1352014542,-0.1165883020,-0.0204088222,-0.1601427644,0.1332627684,-0.0663280189,-0.5192539692,-0.0642980933,-0.1892914921,-0.2908446193,-0.2276112586,0.0022625909,0.2348727286,0.0550857596,-0.1077740937,0.0724524781,-0.0811564699,-0.1227891594,-0.2040644139,-0.3096825182,-0.0821181238,0.0298523642],
  [-0.0219979994,-0.0735332593,-0.1661118865,0.1187807098,-0.2313139588,0.1379121393,-0.1626456529,-0.1306862831,-0.2596812248,-0.0893628076,0.1739192009,0.0958361104,0.1857063472,0.0983618051,-0.1927314848,-0.0582423806,-0.1354507059,0.2356400192,-0.2076291889,0.1583784372,0.1587516218,0.3254724741,-0.0716581047,0.1291785389,0.4059903026,-0.1095065773,-0.2713593543,-0.3458326757,0.1269809604,-0.3204100132,-0.0757777914,0.0773067772],
  [0.0313013494,0.0734305009,0.0418008380,0.3808512986,-0.2212739140,-0.1264244020,-0.1466243267,0.2582320571,0.1599396914,0.0808873400,0.2819415033,-0.2335162610,0.2724173963,0.4177823663,0.2541816831,0.3294693232,-0.1814732999,-0.1175551042,-0.0537235327,0.1466012150,-0.1363659501,0.2240577638,0.2478751242,0.1420787573,0.0909560025,-0.1762425452,-0.1497035176,0.1645867527,0.1341133565,-0.0227953233,-0.0079569798,-0.3577781618],
  [-0.1908381134,0.3359498382,-0.2227081209,0.0458897576,0.1788761765,-0.2262988836,0.0369488187,0.1681168228,0.0024623794,0.5098493695,-0.2935450375,0.1890612841,-0.0305473208,0.1437002122,0.0817400143,-0.1295708865,0.0407461226,-0.3286080658,0.1139641181,0.0533694029,0.1128701791,0.1864298433,0.0652539432,0.0270749498,-0.1540824175,-0.1276715845,0.2097870260,0.2485738844,-0.0421238616,0.0078732371,-0.1575141549,0.2078067213],
  [0.1120939031,0.3417249322,-0.1483753771,-0.0573873632,0.0515885055,0.1172824651,-0.0302651208,-0.1532474458,0.1567680240,0.1318102479,-0.1612292826,0.2415362448,0.2042160630,0.0244962014,0.0162880886,0.0970915109,-0.1554213166,0.0061849980,-0.3711256683,0.1555520892,-0.2773086727,0.2623161077,-0.0580885336,-0.0366582945,-0.1098657325,-0.0522785932,0.2507632971,-0.4397865832,0.2254508734,0.0609317385,-0.2143634409,-0.1896778196],
  [0.2833240032,-0.1671435833,-0.2003507167,0.2039832920,0.0821965262,-0.1633908749,0.0882103294,-0.1933231205,0.1955689937,0.1775938123,0.0877483636,0.0761329159,0.3378460407,0.0101912711,-0.2226598412,0.0787642300,0.3583999574,-0.1779153049,-0.0491280369,0.0723742619,-0.0130309789,-0.1780187488,0.1991475075,0.4197058082,0.0366481468,-0.1639000177,-0.2738201320,0.0633597225,0.1946594715,0.1218127459,-0.0175639391,-0.1041694954],
  [-0.2007371038,0.1475645751,0.1636144966,-0.2672504783,0.2792741954,0.1011338383,0.2797049582,-0.2451664656,-0.2393472493,-0.0339667574,0.0872944072,0.3247619271,0.1362237036,-0.0287261475,-0.1912630051,0.2184937000,-0.1695529521,0.2842105329,-0.0434224606,0.2817464173,0.1214438975,-0.0214774404,-0.0915539414,-0.0544850416,0.0790213048,0.0096122036,-0.1049720198,-0.0272329058,0.0133556435,-0.3542166352,0.0694841594,-0.1731918752],
  [-0.2048112601,-0.2994222939,-0.3171183765,-0.2043340057,-0.1453991085,0.3445706069,-0.0908135995,0.0549867041,0.0709437132,0.0207778886,0.0025924924,-0.2136119604,0.2554056346,0.3547137082,-0.2716441154,-0.1771137714,-0.0220545903,-0.1766212732,0.1014994904,-0.1243561059,0.3942271471,0.2313680202,0.0388497710,0.1861013174,0.2053514272,-0.1009477749,-0.1442881823,-0.0129349548,0.1522106975,0.0234871395,-0.2712718546,-0.1416615248],
  [-0.0310657714,-0.0206842553,0.3626857400,-0.3003573418,0.1517425030,0.2737790942,0.4573009908,-0.1290523857,0.0863211453,0.0973714963,-0.0050271284,0.0131075485,0.1132016256,-0.5098791718,0.0577991009,0.0132766878,-0.1000743806,0.3790867627,-0.1432031244,-0.0814931318,0.3199249506,-0.2051708549,-0.0982653871,-0.0270178076,0.1324202716,0.0289260074,-0.2285060436,-0.0797898620,-0.1044762731,0.0228926502,0.0135564860,0.0756856576],
  [-0.3148414493,0.1869942993,0.0446482860,0.2445323765,0.0812041834,0.1925441623,0.0845417306,0.2360607684,0.0927872434,-0.0190793220,0.4007061124,0.1586953998,0.0998447835,-0.2239676416,-0.2302570790,0.0519682467,0.0242727604,-0.1096332818,0.0254837461,0.2803727090,0.1216897592,-0.2025942653,-0.0197121613,-0.1420201063,-0.0685363114,-0.0746005848,-0.0844366103,-0.2218786776,-0.0225663725,-0.0991023630,-0.5187384486,0.3631634414],
  [0.1876707822,-0.0665211976,0.0046219844,-0.1997717023,0.2998230159,0.3362844288,0.0396076925,0.1795777977,0.0308878981,-0.2363018990,-0.0728985369,0.0264908932,-0.0705800578,-0.0369028114,-0.0179971568,-0.0140776755,0.0104880640,-0.1880480051,-0.1261013448,-0.2268358767,0.1733514667,-0.3718694746,0.2044598013,0.2470666915,-0.0513792783,-0.4601739645,-0.0638712868,-0.0253710411,-0.1777623296,-0.0815691873,0.2608762085,0.1175436229],
  [-0.1669764668,-0.2824183106,-0.0568488538,0.0308247581,0.0873128027,-0.0939753354,0.0812651068,0.3937379718,0.1459616721,-0.0092213126,0.0018882420,-0.0827915147,0.1813350022,-0.0412323400,0.0651357621,0.0160000883,-0.0378442816,-0.2566787899,0.1050920859,-0.0017906792,0.1118975952,-0.0512592271,0.2184889913,-0.1458071917,0.3225328028,-0.3092299402,0.0768572167,0.0888423100,-0.1444552690,0.0614027716,0.2736527324,-0.3918819129],
  [0.0764988437,-0.4407521784,-0.1674781442,0.3909020722,-0.1308341026,-0.2053119987,0.0279386006,0.4289416671,-0.3222275972,-0.2050079703,0.3085811436,0.3289678395,0.0089950291,-0.0943483636,0.4402041137,0.2914240360,0.1897643209,-0.1580297351,0.0551121309,-0.0164192915,0.0895537734,0.3175148368,0.1597345024,-0.1634778380,0.2724120021,0.0475295149,0.1214572191,0.2503504753,-0.2001300156,0.0384511873,-0.0777586773,-0.2771610022],
  [0.0509736799,0.4439846575,-0.3221547008,-0.0811802521,0.1547570825,0.0239024088,0.0337754488,-0.2993635535,0.0406985395,-0.1414380074,-0.4322271347,0.2408590019,0.2156513035,-0.0620173626,-0.0370298959,0.1811427325,-0.0306565147,0.1764203608,-0.0841774568,0.2203317136,0.0261315387,-0.0196651462,-0.2265236676,0.0495728552,-0.1674442589,-0.1159024611,-0.1441394091,-0.1589216590,0.3783507347,0.2820287049,0.2813832760,-0.2161793411],
  [0.0108877486,-0.2286619991,-0.1225227267,-0.1547383219,0.2160682678,-0.0697314590,0.1928448677,0.1099307761,0.4071359634,-0.2615312636,0.2248702496,0.2033643275,-0.1683929563,0.1830978543,0.0627830327,-0.0397068560,-0.1414752454,-0.1279692501,0.3643341660,0.0007860085,0.1399547607,-0.1368547231,-0.1374745667,0.1926004440,-0.2366444618,-0.0734001175,0.0060152533,0.0110923732,-0.0313513353,0.0732235461,0.1202612445,0.1298667938],
  [-0.1766859740,-0.0526330657,0.2196081728,0.1684996337,-0.2352871746,0.0618717149,-0.3440098166,0.0110212173,0.3365303278,-0.1612114310,-0.1469028741,0.1399289817,-0.2748422027,-0.1115848050,0.1094781831,-0.1137658954,-0.0472186953,0.1333918571,0.0800259262,0.0473698825,-0.0630950779,0.0604317598,-0.0218730550,0.0810492337,-0.0887369439,-0.0354922600,-0.1750014722,-0.0061885295,-0.1432767808,-0.1961405873,0.0401562117,0.0382190235],
  [0.1201421544,-0.0963576287,0.0928145498,0.0793412626,0.1333525628,-0.1378940642,0.0703974068,0.0339666121,0.0007782609,-0.3789124191,0.0037560074,0.4162315726,-0.0587583557,-0.1381056905,0.0351592600,0.0499021150,0.1844674200,-0.2123288810,0.1240502223,0.0983176753,-0.0504094362,0.0128292833,0.1818325371,0.1537530571,-0.2062027752,0.1248244718,-0.0114354379,0.0156107740,0.0132906856,-0.0210375916,0.1716302782,-0.0101161217],
  [-0.2085073590,-0.0695669055,0.0346905999,0.3098319471,0.1185336784,-0.1492397189,0.4514808655,0.1107011139,0.2286256105,-0.0317777433,0.1368227005,0.1307337284,0.1930478215,0.2835813165,0.2243062109,-0.0471083149,-0.0394323021,0.0410389230,0.2361968458,-0.0282554757,0.2171369195,0.3115277588,-0.1578519046,-0.1305367947,0.0020637736,0.0519086532,-0.0927312225,0.2541725934,0.0698434189,0.0085739521,-0.1985341609,-0.2326055765],
  [0.0449078195,-0.3769932091,0.1804313064,-0.0282086097,0.5773705244,0.4071677625,-0.0164586026,-0.0269491468,0.1767458916,0.3002822697,-0.2939009070,-0.3079940081,-0.0991605371,0.0981910974,0.0778431147,0.1885259598,-0.2170212716,-0.4093730748,0.1487753987,-0.1472613364,-0.3591887355,0.0215574913,0.1405418515,-0.0197775774,-0.0812102854,-0.0294150934,-0.0655103996,-0.2483337522,-0.1252189130,0.1053742021,0.0426109582,0.0297090579],
  [-0.1779749393,-0.0345967524,-0.3848543763,0.0433570296,0.2247845829,-0.2610803843,-0.0728452578,0.0604348183,0.2215892076,-0.0923845544,-0.1188924164,-0.3372761607,0.0391907357,0.3914621770,0.0510213301,-0.0016282084,0.0710758120,-0.0040974617,-0.1447639614,0.1470256895,0.0438997261,0.0112347743,-0.1720965505,0.2371080518,0.1223526224,-0.1041935682,0.1770992875,-0.1061543003,-0.1381201744,-0.1137100756,-0.3302316070,0.0839246586],
  [-0.3308156133,-0.2168192267,-0.3598109484,0.1153423190,-0.0492152683,-0.0576630346,-0.1868932545,-0.1172201410,0.0837618634,-0.0900398195,0.1147111729,-0.1790192574,0.0107459761,0.3431083858,0.2042350173,-0.0625420213,0.1284147203,-0.0618624650,0.0740644410,-0.0419092812,0.0098804887,0.1080991402,-0.1707626283,-0.1632546037,-0.2125090659,-0.2825466692,-0.2151872069,-0.0050940942,-0.1612079293,0.0823475569,0.0608969256,-0.3480479717],
  [0.1134600565,0.1433303058,-0.1752308458,0.1744128913,-0.1863976568,0.0137761366,-0.2316514403,-0.2460853159,-0.0651657730,-0.0218723938,-0.0216900613,-0.2274643183,-0.0947967395,0.1442905813,0.0697126836,0.0426708572,-0.0430626012,0.1863902807,-0.1586651355,0.0989243090,0.2423062474,0.1475269049,-0.0323525891,-0.0812363401,0.2100587338,-0.2526207864,-0.1714953631,-0.1052278951,0.2160751671,0.0549041629,-0.1282954663,-0.0548551716],
];
const _b1: number[] = [-0.0008678673,-0.0003327616,0.0006561225,0.0050037587,-0.0003875124,0.0000003742,0.0026420392,0.0024308388,0.0001269471,-0.0003472752,0.0017155069,0.0000651700,0.0002885413,0.0050598783,0.0027782074,0.0003766603,0.0028260066,-0.0006838653,0.0029644934,0.0005143204,0.0011119707,0.0020078821,0.0001846165,0.0010004642,0.0013255391,-0.0005294697,0.0000000000,-0.0010526150,-0.0001543753,0.0011824571,0.0007599418,0.0000823712];
const _w2: number[][] = [[-0.1179654226,-0.1510653049,0.2654309869,-0.8996304274,0.4751574397,0.2234492749,0.4362032115,-0.8059236407,0.0530966632,-0.3659325540,-0.4574441314,0.2886743546,0.2936827242,-0.9030751586,-0.8641408682,-0.0588534325,-0.5975350738,0.1058942527,-0.5671861768,-0.0983112231,-0.2528099418,-0.5464329123,-0.0429560095,-0.1339184940,-0.2087808251,-0.1401382536,0.0798811689,0.3538281024,0.0243379455,0.3433579504,-0.4444487989,0.0592626967]];
const _b2: number[] = [-0.0073972987];
const _norm_X_mean: number[] = [0.1567132026,0.7294589281,0.1353362650,0.2465972751,0.2942309976,0.5000000000,1.0000000000,0.1696671844,0.0000000000,0.0000000000,0.0000000000,0.0695652142,1.8510869741,6.5858697891,0.0173913036,0.7380434871,0.7380434871,0.0000000000,0.0000000000,0.0000000000,0.0000000000,0.0695652142,1.8510869741,6.5858697891,0.0173913036,0.7380434871,0.7380434871,0.0000000000];
const _norm_X_std: number[] = [0.1434504837,0.1499476433,0.0000009935,0.0000003229,0.2229220420,0.0000000100,0.0000000100,0.0000005315,0.0000000100,0.0000000100,0.0000000100,0.5228959918,3.0703110695,2.2749149799,0.1307240129,0.4396980703,0.4396980703,0.0000000100,0.0000000100,0.0000000100,0.0000000100,0.5228959918,3.0703110695,2.2749149799,0.1307240129,0.4396980703,0.4396980703,0.0000000100];
const _norm_y_mean = 0.3962661326;
const _norm_y_std = 0.0705244865;

function _relu(x: number): number { return x > 0 ? x : 0; }
function _sigmoid(x: number): number { return x >= 0 ? 1/(1+Math.exp(-x)) : Math.exp(x)/(1+Math.exp(x)); }

function predictInline(xRaw: number[]): number {
  // Normalize input
  const x: number[] = [];
  for (let i = 0; i < 28; i++) {
    x.push((xRaw[i] - _norm_X_mean[i]) / (_norm_X_std[i] + 1e-8));
  }
  // Layer 1: 28 -> 64 (relu)
  const h1: number[] = [];
  for (let j = 0; j < 64; j++) {
    let s = _b0[j];
    for (let i = 0; i < 28; i++) s += x[i] * _w0[i][j];
    h1.push(_relu(s));
  }
  // Layer 2: 64 -> 32 (relu)
  const h2: number[] = [];
  for (let j = 0; j < 32; j++) {
    let s = _b1[j];
    for (let i = 0; i < 64; i++) s += h1[i] * _w1[i][j];
    h2.push(_relu(s));
  }
  // Layer 3: 32 -> 1 (linear)
  let s = _b2[0];
  for (let i = 0; i < 32; i++) s += h2[i] * _w2[0][i];
  return s * _norm_y_std + _norm_y_mean;
}

// ============================================================
// v13: Transition Planner（核心新模块）
// ============================================================

export type TransitionType = "whip" | "fade" | "zoom";

/**
 * 单个镜头的 transition 决策
 * microCut: shot 内部的 micro-cut（v13 新增，在 shot 60%~62% 处做微冲击）
 */
export interface TransitionDecision {
  shotIndex: number;
  type: TransitionType;
  /** shot 内部 micro-cut 的时间点（0~1，相对于 shot 长度） */
  microCutAt?: number;
  /** micro-cut 的强度（0~1） */
  microCutIntensity?: number;
}

/**
 * 全量 transition 规划（整个 timeline 一次性算好）
 * Map: shotIndex → TransitionDecision
 */
export type TransitionPlan = Map<number, TransitionDecision>;

/**
 * v13: 剪辑预算系统（Budget-based Editorial Policy）
 *
 * whip   = -3 budget（高消耗）
 * zoom   = -1 budget
 * fade   = +0.5 budget（恢复）
 *
 * budget 耗尽 → 强制 fade/zoom
 * budget 缓慢自动恢复（+0.5/shot）
 *
 * cooldown: cooldown > 0 时禁止 whip
 */
interface EditorState {
  budget: number;
  cooldown: number; // frames
  lastTransition: TransitionType;
}

const MAX_BUDGET = 6;
const BUDGET_REGEN = 0.5;      // 每 shot 恢复 0.5 budget
const WHIP_COST = 3;
const ZOOM_COST = 1;
const COOLDOWN_FRAMES = 8;     // whip 后强制 cooldown 8 帧（≈ 0.27秒@30fps）
const MAX_CONSECUTIVE_WHIP = 2;

// ============================================================
// v14: Global Energy Curve & Optimizer Helpers
// ============================================================

/**
 * v14: 全局连续能量曲线
 *
 * 将离散的 per-shot emotion 采样 → 连续平滑曲线
 * 使用分段线性插值（spline 也可以但当前用 smoothstep 效果更好）
 *
 * 用于：
 *   - 全局节奏密度分析
 *   - micro-cut 位置语义锚点
 *   - whip transition 与高能量区间的对齐评分
 */
function buildGlobalEnergyCurve(
  shots: Shot[],
  emotions: number[],
  fps: number
): Array<{ frame: number; energy: number }> {
  if (shots.length === 0) return [];

  const samples: Array<{ frame: number; energy: number }> = [];
  // 在每个 shot 的 20%, 50%, 80% 处采样（不用 midpoint，用三点更平滑）
  for (let i = 0; i < shots.length; i++) {
    const shot = shots[i];
    const emotion = emotions[i] ?? 0.5;
    const f20 = shot.start + shot.duration * 0.2;
    const f50 = shot.start + shot.duration * 0.5;
    const f80 = shot.start + shot.duration * 0.8;
    // 三点均值，减少异常值影响
    const avgEmotion = emotion;
    samples.push({ frame: f20, energy: avgEmotion });
    samples.push({ frame: f50, energy: avgEmotion });
    samples.push({ frame: f80, energy: avgEmotion });
  }

  // 按 frame 排序（理论上已经是有序的）
  return samples.sort((a, b) => a.frame - b.frame);
}

/**
 * v14: 在 shot 内找能量峰值帧
 *
 * 用折返方式找能量最高的采样点 frame
 * 作为 semantic micro-cut anchor
 *
 * 效果：micro-cut 不再是"第 60% 帧"
 * 而是"这个 shot 里能量最高的时刻"
 */
function findEmotionPeakFrame(
  shot: Shot,
  energyCurve: Array<{ frame: number; energy: number }>,
  defaultFrac: number,
  fps: number
): number {
  const inShot = energyCurve.filter(
    (p) => p.frame > shot.start + fps * 0.1 && p.frame < shot.start + shot.duration - fps * 0.1
  );
  if (inShot.length === 0) {
    return shot.start + shot.duration * defaultFrac;
  }
  const peak = inShot.reduce((best, p) => (p.energy > best.energy ? p : best));
  return peak.frame;
}

/**
 * v14: Whip 密度约束（全局窗口控制）
 *
 * 保证：每 150 帧（约 5 秒 @30fps）最多 1 次 whip
 * 防止：连续 whip 集中在某一时段导致"节奏窒息"
 *
 * 策略：贪婪移除最低强度的 whip，直到满足密度约束
 */
function enforceWhipDensityConstraint(
  plan: TransitionPlan,
  shots: Shot[],
  fps: number
): void {
  const WINDOW_FRAMES = 150;  // 150帧 ≈ 5秒 @30fps
  const MAX_WHIP_PER_WINDOW = 1;

  // 收集所有 whip transition 的 shotIndex
  const whipIndices: number[] = [];
  plan.forEach((dec, idx) => {
    if (dec.type === "whip") whipIndices.push(idx);
  });

  // 滑动窗口检测：统计每个窗口内的 whip 数量
  function countWhipsInWindow(startFrame: number): number {
    return whipIndices.filter((i) => {
      const t = shots[i].start;
      return t >= startFrame && t < startFrame + WINDOW_FRAMES;
    }).length;
  }

  // 持续收紧直到满足密度约束
  let changed = true;
  while (changed) {
    changed = false;
    const sortedWhips = [...whipIndices].sort((a, b) => {
      // 按 shot 能量降序：能量高的 whip 优先保留
      const aDec = plan.get(a)!;
      const bDec = plan.get(b)!;
      return (bDec.microCutIntensity ?? 0) - (aDec.microCutIntensity ?? 0);
    });

    for (const idx of sortedWhips) {
      const startFrame = shots[idx].start;
      if (countWhipsInWindow(startFrame) > MAX_WHIP_PER_WINDOW) {
        // 强制降级为 zoom
        const dec = plan.get(idx)!;
        plan.set(idx, { ...dec, type: "zoom" });
        whipIndices.splice(whipIndices.indexOf(idx), 1);
        changed = true;
      }
    }
  }
}

/**
 * v14: Plan 全局质量评分（用于调试和未来优化方向）
 *
 * 评分维度：
 *   1. 多样性（transition type 分布是否均匀）
 *   2. Budget 利用率（是否在 budget 范围内高效消耗）
 *   3. 节奏对齐（whip 是否对齐高能量区间）
 */
function scorePlan(
  plan: TransitionPlan,
  shots: Shot[],
  energyCurve: Array<{ frame: number; energy: number }>,
  _fps: number
): number {
  if (plan.size === 0) return 0;

  // 1. 多样性评分（0~1，越高越好）
  const typeCount = { whip: 0, fade: 0, zoom: 0 };
  plan.forEach((dec) => { typeCount[dec.type]++; });
  const total = plan.size;
  const typeProbs = [typeCount.whip / total, typeCount.fade / total, typeCount.zoom / total];
  const diversity = 1 - Math.max(...typeProbs); // 最高类型占比越低，多样性越高

  // 2. Budget 利用率（whip 是高消耗高回报）
  const whipRatio = typeCount.whip / total;
  const budgetScore = Math.min(1, whipRatio * 2); // whip 占比 50% 时得满分

  // 3. 节奏对齐（whip 落在高能量区间的比例）
  const energyThreshold = 0.65;
  let alignmentHits = 0;
  plan.forEach((dec) => {
    if (dec.type === "whip") {
      const peakFrame = findEmotionPeakFrame(shots[dec.shotIndex], energyCurve, 0.6, _fps);
      const peakEnergy = energyCurve.find((p) => p.frame === peakFrame)?.energy ?? 0;
      if (peakEnergy >= energyThreshold) alignmentHits++;
    }
  });
  const alignmentScore = typeCount.whip > 0 ? alignmentHits / typeCount.whip : 1;

  // 加权总分
  return diversity * 0.4 + budgetScore * 0.3 + alignmentScore * 0.3;
}

// ============================================================
// ============================================================
// v16: Global Sequence Optimization (Full-Objective Editor)
// ============================================================

/**
 * v16: Beam 结构（改为 cost-based，score 从全局评估得到）
 *
 * 核心变化（相比 v15）：
 *   v15: beam.score = 增量累积（greedy accumulation）
 *   v16: beam.score = 待评估（beam search 时为 pending，
 *                               最终在 evaluateFullSequence 中统一计算）
 */
interface Beam {
  plan: TransitionPlan;
  state: EditorState;
  pendingCost: number;
  consecutiveWhip: number;
}

interface MctsNode {
  shotIndex: number;
  abstractionKey: string;  // v17.1: 抽象状态键，用于 Q-Table 共享
  state: EditorState;
  parent: MctsNode | null;
  children: MctsNode[];
  visits: number;
  Q: number;
  consecutiveWhip: number;
  type: TransitionType | null;
  plan: TransitionPlan;
}

const BEAM_WIDTH = 4;
const TRANSITION_TYPES: TransitionType[] = ["whip", "fade", "zoom"];

// ============================================================
// v17.1: State Abstraction Layer（让 MCTS 从"记忆型"变"泛化型"）
// ============================================================

/**
 * v17.1: State Abstraction — 核心升级
 *
 * 问题：tabular MCTS 每个 shot 都是独立 state，即使 energy/emotion 很相似也不会共享 Q
 *
 * 解决：离散化 + hash，让相似 state 共享 Q-value
 *
 * abstraction key 生成：
 *   energy_bucket  = round(energy / 0.10)  → 0~10 的离散桶
 *   emotion_bucket  = round(emotion / 0.10) → 0~10 的离散桶
 *   rhythm_phase    = floor(frame / 150)    → 每 150 帧（~5s@30fps）为同一相位
 *   lastTransition  = 直接字符串
 *   cooldown_bucket = round(cooldown / 4)  → 每 4 帧一个桶
 *
 * 效果：
 *   - 相似 state 共享 Q → search tree 压缩 5-10x
 *   - rollout variance ↓
 *   - MCTS 从"记忆型"变"泛化型"
 */
function discretize(value: number, step: number): number {
  return Math.round(value / step);
}

/**
 * v19.6b: Soft binning for energy — preserves fractional position within coarse bin
 * CRITICAL: use FLOOR not ROUND for coarse bin, so energies within [b*step, (b+1)*step)
 *   all share the same coarse bin but have different fractional keys.
 * Example: 0.62 → floor(0.62/0.10)=6, frac=(0.62-0.60)/0.10=0.2 → 6+0.1=6.1
 *          0.64 → floor(0.64/0.10)=6, frac=(0.64-0.60)/0.10=0.4 → 6+0.2=6.2
 *          0.65 → floor(0.65/0.10)=6, frac=(0.65-0.60)/0.10=0.5 → 6+0.25=6.25
 *   → 0.62 and 0.65 (within same 0.10 bin) now have different keys!
 *   → Whip@0.62 vs Whip@0.65 are distinguishable in Q-table.
 */
function softDiscretizeEnergy(energy: number): number {
  const coarseBin = Math.floor(energy / 0.10);  // FLOOR: 0.64 and 0.65 both in bin 6
  const frac = (energy - coarseBin * 0.10) / 0.10;  // fractional position within bin [0, 1)
  return coarseBin + frac * 0.5;  // fractional offset ∈ [0, 0.5)
}

function getStateAbstractionKey(
  energy: number,
  emotion: number,
  frame: number,
  lastTransition: TransitionType,
  cooldown: number,
  consecutiveWhip: number
): string {
  // v19.6b fix: refine abstraction granularity to resolve plan collapse
  // OLD: energy bucket = 0.10 → whip@0.62 and whip@0.68 merged into same state
  // v19.6b 1st attempt: energy bucket = 0.05 → 2x resolution but over-fragmented
  // v19.6b FINAL: soft binning — coarse 0.10 bucket + fractional offset per energy
  //   0.62 → 6.10 (coarse bin 6 + frac 0.2 → ×0.5 = 0.1), 0.68 → 6.40
  //   → Same coarse bucket as 0.10, but continuous resolution within it
  //   → Avoids aliasing AND fragmentation simultaneously
  const energyBucket = softDiscretizeEnergy(energy);  // soft binning: 6.1, 6.4, etc.
  const emotionBucket = discretize(emotion, 0.10);  // 0~10
  const rhythmPhase = Math.floor(frame / 150);        // 每 150 帧一个相位
  const cooldownBucket = discretize(cooldown, 4);    // 每 4 帧一个桶
  return `${energyBucket}|${emotionBucket}|${rhythmPhase}|${lastTransition}|${cooldownBucket}|${consecutiveWhip}`;
}

// 全局 Q-Table：abstraction key → Q-value（跨节点共享）
// v19.6b: MUST be reset per episode — old Q values are from previous reward function
//   and will cause corr(Q,reward) = -0.95 even with correct rollout + reward.
const globalQTable: Record<string, { visits: number; Q: number }> = {};

function resetGlobalQTable(): void {
  for (const key of Object.keys(globalQTable)) {
    delete globalQTable[key];
  }
}

// ── abstraction key 的 rollout 统计（用于评估）─────────────
function getAbstractQ(abstractKey: string): number {
  return globalQTable[abstractKey]?.Q ?? 0.5;  // 未知 state 返回中性 0.5
}

function updateAbstractQ(abstractKey: string, reward: number): void {
  if (!globalQTable[abstractKey]) {
    globalQTable[abstractKey] = { visits: 0, Q: 0.5 };
  }
  const entry = globalQTable[abstractKey];
  entry.visits++;
  // 增量平均
  entry.Q = entry.Q + (reward - entry.Q) / entry.visits;
}

// ============================================================
// v18: Reward Data Collector（把剪辑系统变成可训练环境）
// ============================================================

/**
 * v18: Reward Data Infrastructure
 *
 * 本质：将"剪辑优化系统"升级为"强化学习训练数据生成器"
 *
 * 收集的数据：
 *   1. best plan + reward（被选中的）
 *   2. rejected alternatives（被拒绝的 beam）→ 用于 future preference learning
 *   3. MCTS 统计（visit count, Q variance）→ 用于 uncertainty-aware reward model
 *
 * 输出格式：JSONL（每行一个 complete episode）
 * 用于未来训练 neural reward model：
 *   F(π) ≈ learned_reward_model(π, context)
 */
interface RewardFeatures {
  energy_alignment: number;
  entropy: number;
  pacing_smoothness: number;
  micro_cut_semantic: number;
  /** v19.6b: how well each transition type matches the shot's energy level */
  energy_transition_alignment: number;
}

interface RewardContext {
  shot_count: number;
  fps: number;
  duration_frames: number;
  emotion_histogram: number[];
  energy_histogram: number[];
}

interface RewardPlanEntry {
  shot: number;
  type: TransitionType;
  microCutAt?: number;
  microCutIntensity?: number;
}

interface MctsStats {
  avgVisits: number;
  qVariance: number;
  totalNodes: number;
  rootChildren: number;
}

interface AlternativePlan {
  /** Optional name: "all-zoom" | "all-fade" | "anti-energy" | "alternating" | undefined */
  name?: string;
  plan: RewardPlanEntry[];
  reward: number;
  features: RewardFeatures;
  qValue: number;
  visits: number;
}

interface RewardDataEntry {
  id: string;
  timestamp: number;
  selected_plan: RewardPlanEntry[];
  selected_reward: number;
  selected_features: RewardFeatures;
  alternatives: AlternativePlan[];
  mcts_stats: MctsStats;
  context: RewardContext;
  /** v19.6b: reward function version — used to filter corrupted vs clean data */
  reward_version: string;
}

class RewardDataCollector {
  private entries: RewardDataEntry[] = [];
  private episodeId: string = "";

  resetEpisode(): void {
    this.episodeId = `episode_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
    this.entries = [];
  }

  collect(
    selectedPlan: TransitionPlan,
    selectedReward: number,
    selectedFeatures: RewardFeatures,
    alternatives: AlternativePlan[],
    shots: Shot[],
    emotions: number[],
    fps: number
  ): void {
    // 构建 emotion histogram (10 buckets)
    const emotionHist = new Array(10).fill(0);
    for (const e of emotions) {
      const bucket = Math.min(9, Math.floor(e * 10));
      emotionHist[bucket]++;
    }

    // 构建 energy histogram from shots
    const energyHist = new Array(10).fill(0);
    for (const shot of shots) {
      const avgEnergy = emotions[shots.indexOf(shot)] ?? 0.5;
      const bucket = Math.min(9, Math.floor(avgEnergy * 10));
      energyHist[bucket]++;
    }

    const planEntries: RewardPlanEntry[] = [];
    selectedPlan.forEach((v, k) => {
      planEntries.push({ shot: k, type: v.type, microCutAt: v.microCutAt, microCutIntensity: v.microCutIntensity });
    });
    planEntries.sort((a, b) => a.shot - b.shot);

    const entry: RewardDataEntry = {
      id: `${this.episodeId}_${this.entries.length}`,
      timestamp: Date.now(),
      selected_plan: planEntries,
      selected_reward: selectedReward,
      selected_features: selectedFeatures,
      alternatives,
      mcts_stats: { avgVisits: 0, qVariance: 0, totalNodes: 0, rootChildren: 0 },
      context: {
        shot_count: shots.length,
        fps,
        duration_frames: shots.reduce((sum, s) => sum + s.duration, 0),
        emotion_histogram: emotionHist,
        energy_histogram: energyHist,
      },
      reward_version: "v19.6b_clean",
    };

    this.entries.push(entry);
  }

  // v18: 收集 MCTS 统计（需在 MCTS 完成后调用）
  setMctsStats(root: MctsNode): void {
    if (this.entries.length === 0) return;

    const lastEntry = this.entries[this.entries.length - 1];
    const visits: number[] = [];
    let totalQ = 0;
    let nodeCount = 0;

    // BFS 遍历所有节点收集统计
    const queue: MctsNode[] = [root];
    while (queue.length > 0) {
      const node = queue.shift()!;
      visits.push(node.visits);
      totalQ += node.Q;
      nodeCount++;
      for (const child of node.children) {
        queue.push(child);
      }
    }

    const avgVisits = visits.reduce((a, b) => a + b, 0) / (visits.length || 1);
    const avgQ = totalQ / (nodeCount || 1);
    const qVariance = visits.length > 0
      ? visits.reduce((sum, v, i) => sum + Math.pow(nodeCount > 0 ? totalQ / nodeCount : 0 - v, 2), 0) / visits.length
      : 0;

    lastEntry.mcts_stats = {
      avgVisits,
      qVariance,
      totalNodes: nodeCount,
      rootChildren: root.children.length,
    };
  }

  getEntries(): RewardDataEntry[] {
    return this.entries;
  }

  exportJSONL(): string {
    return this.entries.map(e => JSON.stringify(e)).join('\n');
  }
}

// 全局实例（每个视频生成任务一个 episode）
let rewardCollector = new RewardDataCollector();

/**
 * v18: 获取已收集的 Reward Data（供 server 端 flush 到 JSONL）
 */
export function getRewardData(): string {
  return rewardCollector.exportJSONL();
}

/**
 * v18: 获取原始 entries（供 server 做 intermediate 分析）
 */
export function getRewardEntries(): RewardDataEntry[] {
  return rewardCollector.getEntries();
}

/**
 * v16: Pure Decision Function（无评分，纯逻辑）
 *
 * 给定当前 state + emotion + beat，输出一个合法的 transition type
 * 不返回分数，只返回决策结果和更新后的状态
 *
 * 与 v15 的本质区别：
 *   v15: 评估每个候选的增量 score → 搜索空间被 score 引导
 *   v16: 只返回合法决策 → 评分全部推迟到全局评估
 */
function decideTransition(
  type: TransitionType,
  state: EditorState,
  emotion: number,
  beat: number,
  consecutiveWhip: number
): { type: TransitionType; newState: EditorState; newConsecutiveWhip: number } {
  const rhythmBoost = beat > 0.6 ? 1 : beat < -0.4 ? -1 : 0;

  let finalType = type;
  if (state.cooldown > 0 && finalType === "whip") {
    finalType = "zoom";
  }

  if (finalType === "whip" && state.budget < WHIP_COST) {
    finalType = state.budget >= ZOOM_COST ? "zoom" : "fade";
  }

  if (finalType === state.lastTransition) {
    if (finalType === "whip") finalType = "zoom";
    else if (finalType === "zoom") finalType = "fade";
    else finalType = "zoom";
  }

  let newConsecutiveWhip = consecutiveWhip;
  if (finalType === "whip") {
    newConsecutiveWhip++;
    if (newConsecutiveWhip >= MAX_CONSECUTIVE_WHIP) {
      finalType = "zoom";
      newConsecutiveWhip = 0;
    }
  } else {
    newConsecutiveWhip = 0;
  }

  const newState: EditorState = { ...state };
  if (finalType === "whip") {
    newState.budget -= WHIP_COST;
    newState.cooldown = COOLDOWN_FRAMES;
  } else if (finalType === "zoom") {
    newState.budget -= ZOOM_COST;
  } else {
    newState.budget = Math.min(MAX_BUDGET, newState.budget + BUDGET_REGEN);
  }
  newState.lastTransition = finalType;

  return { type: finalType, newState, newConsecutiveWhip };
}

/**
 * v16: 统一全局目标函数（Full-Sequence Scoring）
 *
 * 这是 v16 的核心创新：
 *   不是增量累积 score，而是在完整序列上统一评估全局目标
 *
 * Score 维度：
 *   1. Energy Alignment - whip 是否落在高能量区间
 *   2. Rhythm Entropy - transition type 分布的熵
 *   3. Pacing Smoothness - whip 在时间轴上的分布均匀程度
 *   4. Micro-cut Semantic - micro-cut 是否落在能量峰值
 */
function evaluateFullSequence(
  plan: TransitionPlan,
  energyCurve: Array<{ frame: number; energy: number }>,
  shots: Shot[],
  emotions: number[],
  fps: number
): number {
  if (plan.size === 0) return 0;
  const WINDOW_FRAMES = 150;

  // ── v19.6b: Continuous Energy Alignment (sigmoid-scaled) ────────
  // Continuously varies with energy. whip: sigmoid((peakE-0.5)*4)*0.5 ∈ [0, 0.5]
  //   fade: (0.3-|peakE-0.5|)/0.3 * 0.3 ∈ [0, 0.3]
  // No clamp → full dynamic range preserved.
  const shotIndices = [...plan.keys()].sort((a, b) => a - b);
  let energyScoreSum = 0;
  let energyCount = 0;
  plan.forEach((dec) => {
    const shot = shots[dec.shotIndex];
    if (!shot) return;
    const peakFrame = findEmotionPeakFrame(shot, energyCurve, 0.6, fps);
    const peakEnergy = energyCurve.find(
      (p) => Math.abs(p.frame - peakFrame) < fps * 0.5
    )?.energy ?? emotions[dec.shotIndex] ?? 0.5;
    if (dec.type === "whip") {
      // sigmoid((peakE-0.5)*4) maps [-0.5,+0.5] → [0.12,0.88]; ×0.5 → [0.06,0.44]
      const e = Math.max(-0.5, Math.min(0.5, peakEnergy - 0.5));
      energyScoreSum += 0.5 * (1 / (1 + Math.exp(-e * 4)));
      energyCount++;
    } else if (dec.type === "fade") {
      energyScoreSum += (0.3 - Math.abs(peakEnergy - 0.5)) / 0.3 * 0.3;
      energyCount++;
    }
  });
  // No clamp — let energy score use full [0, 1] range naturally
  const energyAlignmentScore = energyCount > 0 ? energyScoreSum / energyCount : 0;

  // ── v20: transitionSuitability — per-shot Gaussian scoring + variance penalty
  // Key insight: each transition type has an optimal energy level (whip=high, zoom=medium, fade=low).
  // Using Gaussian lets us score "how correct" each transition is, then penalize inconsistency.
  let transScores: number[] = [];
  for (const [, dec] of plan.entries()) {
    const shot = shots[dec.shotIndex];
    if (!shot) continue;
    const peakFrame = findEmotionPeakFrame(shot, energyCurve, 0.6, fps);
    const e = energyCurve.find(p => Math.abs(p.frame - peakFrame) < fps * 0.5)?.energy
      ?? emotions[dec.shotIndex] ?? 0.5;
    let s = 0.5;
    if (dec.type === "whip") {
      s = Math.exp(-Math.pow(e - 0.8, 2) / (2 * 0.04)); // sigma=0.2
    } else if (dec.type === "zoom") {
      s = Math.exp(-Math.pow(e - 0.5, 2) / (2 * 0.0625)); // sigma=0.25
    } else if (dec.type === "fade") {
      s = Math.exp(-Math.pow(e - 0.2, 2) / (2 * 0.04)); // sigma=0.2
    }
    transScores.push(s);
  }
  const tMean = transScores.length > 0 ? transScores.reduce((a, b) => a + b, 0) / transScores.length : 0.5;
  const tVar = transScores.length > 0
    ? transScores.reduce((sum, s) => sum + Math.pow(s - tMean, 2), 0) / transScores.length : 0;
  // High mean + low variance = plan is consistently well-aligned → reward it
  const transitionSuitability = 0.7 * tMean + 0.3 * (1 - Math.sqrt(tVar));

  // ── v20: structurePatternScore — penalizes repetitive and mechanical patterns
  // Key: not "how diverse" but "how pattern-abiding"
  const types = [...plan.entries()].sort((a, b) => a[0] - b[0]).map(e => e[1].type);
  let repeat = 0, alt = 0;
  for (let i = 0; i < types.length - 1; i++) {
    if (types[i] === types[i + 1]) repeat++;
    if (i < types.length - 2 && types[i] === types[i + 2]) alt++;
  }
  const repeatRatio = repeat / (types.length - 1 || 1);
  const altRatio = alt / (types.length - 2 || 1);
  // Penalize long runs (AAAA) and mechanical alternation (ABAB)
  const structurePatternScore = Math.max(0, 1 - 0.6 * repeatRatio - 0.3 * altRatio);

  // ── v20: Pacing = Gaussian on shot-duration std
  const durations: number[] = [];
  for (const idx of shotIndices) {
    const shot = shots[idx];
    if (shot) durations.push(shot.duration);
  }
  const meanDur = durations.reduce((a, b) => a + b, 0) / (durations.length || 1);
  const varDur = durations.reduce((sum, d) => sum + Math.pow(d - meanDur, 2), 0) / (durations.length || 1);
  const stdDur = Math.sqrt(varDur);
  const pacingScore = Math.exp(-Math.pow(Math.min(1, stdDur / 3.0) - 0.5, 2) / (2 * 0.0625));

  // ── v20: Micro-cut = exp(-avgDistanceToPeak*4)
  let microDistSum = 0, microCount = 0;
  for (const dec of plan.values()) {
    const shot = shots[dec.shotIndex];
    if (!shot || dec.microCutAt === undefined) continue;
    const peakFrame = findEmotionPeakFrame(shot, energyCurve, 0.6, fps);
    const peakFrac = shot.duration > 0 ? (peakFrame - shot.start) / shot.duration : 0.6;
    microDistSum += Math.abs(dec.microCutAt - peakFrac);
    microCount++;
  }
  const avgMicroDist = microCount > 0 ? microDistSum / microCount : 0.5;
  const microCutScore = Math.exp(-avgMicroDist * 4);

  // v20: Final reward — 5 components with clear semantic roles
  return transitionSuitability * 0.35 +
    structurePatternScore * 0.25 +
    pacingScore * 0.20 +
    energyAlignmentScore * 0.10 +
    microCutScore * 0.10;
}

/**
 * v18: Extract 4-dim reward feature breakdown
 * 用于 reward data collection（不改变 evaluateFullSequence 行为）
 */
function computeRewardFeatures(
  plan: TransitionPlan,
  energyCurve: Array<{ frame: number; energy: number }>,
  shots: Shot[],
  emotions: number[],
  fps: number
): { features: RewardFeatures; score: number } {
  if (plan.size === 0) {
    return { features: { energy_alignment: 0, entropy: 0, pacing_smoothness: 0, micro_cut_semantic: 0, energy_transition_alignment: 0 }, score: 0 };
  }
  const WINDOW_FRAMES = 150;

  // ── v19.6b: Continuous Energy Alignment (same as evaluateFullSequence)
  const shotIndices = [...plan.keys()].sort((a, b) => a - b);
  let energyScoreSum = 0;
  let energyCount = 0;
  plan.forEach((dec) => {
    const shot = shots[dec.shotIndex];
    if (!shot) return;
    const peakFrame = findEmotionPeakFrame(shot, energyCurve, 0.6, fps);
    const peakEnergy = energyCurve.find(
      (p) => Math.abs(p.frame - peakFrame) < fps * 0.5
    )?.energy ?? emotions[dec.shotIndex] ?? 0.5;
    if (dec.type === "whip") {
      const e = Math.max(-0.5, Math.min(0.5, peakEnergy - 0.5));
      energyScoreSum += 0.5 * (1 / (1 + Math.exp(-e * 4)));
      energyCount++;
    } else if (dec.type === "fade") {
      energyScoreSum += (0.3 - Math.abs(peakEnergy - 0.5)) / 0.3 * 0.3;
      energyCount++;
    }
  });
  const energyAlignmentScore = energyCount > 0 ? energyScoreSum / energyCount : 0;

  // ── v20: transitionSuitability (same as evaluateFullSequence)
  let transScores: number[] = [];
  for (const [, dec] of plan.entries()) {
    const shot = shots[dec.shotIndex];
    if (!shot) continue;
    const peakFrame = findEmotionPeakFrame(shot, energyCurve, 0.6, fps);
    const e = energyCurve.find(p => Math.abs(p.frame - peakFrame) < fps * 0.5)?.energy
      ?? emotions[dec.shotIndex] ?? 0.5;
    let s = 0.5;
    if (dec.type === "whip") {
      s = Math.exp(-Math.pow(e - 0.8, 2) / (2 * 0.04));
    } else if (dec.type === "zoom") {
      s = Math.exp(-Math.pow(e - 0.5, 2) / (2 * 0.0625));
    } else if (dec.type === "fade") {
      s = Math.exp(-Math.pow(e - 0.2, 2) / (2 * 0.04));
    }
    transScores.push(s);
  }
  const tMean = transScores.length > 0 ? transScores.reduce((a, b) => a + b, 0) / transScores.length : 0.5;
  const tVar = transScores.length > 0
    ? transScores.reduce((sum, s) => sum + Math.pow(s - tMean, 2), 0) / transScores.length : 0;
  const transitionSuitability = 0.7 * tMean + 0.3 * (1 - Math.sqrt(tVar));

  // ── v20: structurePatternScore (same as evaluateFullSequence)
  const types = [...plan.entries()].sort((a, b) => a[0] - b[0]).map(e => e[1].type);
  let repeat = 0, alt = 0;
  for (let i = 0; i < types.length - 1; i++) {
    if (types[i] === types[i + 1]) repeat++;
    if (i < types.length - 2 && types[i] === types[i + 2]) alt++;
  }
  const repeatRatio = repeat / (types.length - 1 || 1);
  const altRatio = alt / (types.length - 2 || 1);
  const structurePatternScore = Math.max(0, 1 - 0.6 * repeatRatio - 0.3 * altRatio);

  // ── v20: pacing = Gaussian (same as evaluateFullSequence)
  const durations: number[] = [];
  for (const idx of shotIndices) {
    const shot = shots[idx];
    if (shot) durations.push(shot.duration);
  }
  const meanDur = durations.reduce((a, b) => a + b, 0) / (durations.length || 1);
  const varDur = durations.reduce((sum, d) => sum + Math.pow(d - meanDur, 2), 0) / (durations.length || 1);
  const stdDur = Math.sqrt(varDur);
  const pacingScore = Math.exp(-Math.pow(Math.min(1, stdDur / 3.0) - 0.5, 2) / (2 * 0.0625));

  // ── v20: micro-cut = exp(-avgDistToPeak*4) (same as evaluateFullSequence)
  let microDistSum = 0, microCount = 0;
  for (const dec of plan.values()) {
    const shot = shots[dec.shotIndex];
    if (!shot || dec.microCutAt === undefined) continue;
    const peakFrame = findEmotionPeakFrame(shot, energyCurve, 0.6, fps);
    const peakFrac = shot.duration > 0 ? (peakFrame - shot.start) / shot.duration : 0.6;
    microDistSum += Math.abs(dec.microCutAt - peakFrac);
    microCount++;
  }
  const avgMicroDist = microCount > 0 ? microDistSum / microCount : 0.5;
  const microCutScore = Math.exp(-avgMicroDist * 4);

  // v20: same as evaluateFullSequence
  const score = transitionSuitability * 0.35 +
    structurePatternScore * 0.25 +
    pacingScore * 0.20 +
    energyAlignmentScore * 0.10 +
    microCutScore * 0.10;

  return {
    features: {
      energy_alignment: energyAlignmentScore,
      entropy: transitionSuitability,
      pacing_smoothness: pacingScore,
      micro_cut_semantic: microCutScore,
      energy_transition_alignment: structurePatternScore,
    },
    score,
  };
}

/**
 * v19.6b: Apply post-processing to a plan (micro-cut anchoring + whip density)
 * Both selected and alternatives must use this SAME post-processing before
 * computeRewardFeatures, otherwise reward is computed on inconsistent plans
 * (selected had micro-cut data, alternatives didn't → micro_cut_semantic=0.5 vs 0).
 */
function postProcessPlan(
  plan: TransitionPlan,
  shots: Shot[],
  emotions: number[],
  energyCurve: Array<{ frame: number; energy: number }>,
  fps: number,
): void {
  // ── 后处理 1: micro-cut 语义锚定 ─────────────────────────
  for (let i = 0; i < shots.length - 1; i++) {
    if (!plan.has(i)) continue;
    const shot = shots[i];
    const emotion = emotions[i] ?? 0.5;
    const peakFrame = findEmotionPeakFrame(shot, energyCurve, 0.6, fps);
    const microCutAt = shot.duration > 0
      ? Math.max(0.55, Math.min(0.9, (peakFrame - shot.start) / shot.duration))
      : 0.60;
    const peakEnergy = energyCurve.find(
      (p) => Math.abs(p.frame - peakFrame) < fps * 0.5
    )?.energy ?? emotion;
    const microCutIntensity = peakEnergy * 0.14;

    const existing = plan.get(i)!;
    plan.set(i, { ...existing, microCutAt, microCutIntensity });
  }

  // ── 后处理 2: Whip 密度约束（硬约束）────────────────────
  enforceWhipDensityConstraint(plan, shots, fps);
}

/**
 * v18: Backtrack full plan from any MCTS child node
 * 用于从 partial node 重建完整 transition plan
 */
function backtrackPlan(node: MctsNode, shots: Shot[]): TransitionPlan {
  const plan = new Map<number, TransitionDecision>();

  // 从叶节点回溯到根
  let cur: MctsNode | null = node;
  while (cur !== null && cur.type !== null) {
    plan.set(cur.shotIndex, { shotIndex: cur.shotIndex, type: cur.type });
    cur = cur.parent;
  }

  // 对尚未填充的 shot，填充 zoom
  for (let i = 0; i < shots.length - 1; i++) {
    if (!plan.has(i)) {
      plan.set(i, { shotIndex: i, type: "zoom" });
    }
  }

  return plan;
}

/**
 * v16: Monte Carlo Rollout（简化版向前模拟）
 *
 * 当 beam 尚未覆盖完整 timeline 时，用 rollout 估算完整 score
 * 策略：对当前 beam 的 state，假设剩余 shot 使用 zoom，计算下界
 */
function rolloutEstimate(
  beam: Beam,
  shots: Shot[],
  emotions: number[],
  energyCurve: Array<{ frame: number; energy: number }>,
  fps: number,
  currentIdx: number
): number {
  const fullPlan = new Map(beam.plan);
  for (let i = currentIdx; i < shots.length - 1; i++) {
    if (!fullPlan.has(i)) {
      fullPlan.set(i, { shotIndex: i, type: "zoom" });
    }
  }
  return evaluateFullSequence(fullPlan, energyCurve, shots, emotions, fps);
}

/**
 * v16: Beam Search with Full-Sequence Evaluation
 *
 * 相比 v15 的本质变化：
 *   v15: beam.score = 增量累积（greedy）
 *   v16: beam.score = evaluateFullSequence(plan)（全局评估）
 *   v15: 剪枝用累积分数（误导性的近期偏差）
 *   v16: 剪枝用 rollout 估算（全局 score 的近似）
 */

/**
 * v17: MCTS-UCT Search + Stochastic Rollout
 *
 * v17 在 v16 基础上做本质跃迁：
 *
 *   v16: Beam Search（横向并行，只保留最优路径）
 *         beam.score = rolloutEstimate（近似全局）
 *         无 exploration term
 *         无 visit statistics
 *
 *   v17: Monte Carlo Tree Search + UCT（真正的树搜索）
 *         ① Node tree 替代 Beam[]（树结构替代列表）
 *         ② UCT selection：Q + c·√(ln(N_parent)/N_child)（探索+利用平衡）
 *         ③ Backpropagation：更新 visit count + Q-value
 *         ④ Stochastic rollout：非确定性策略，不再是"全填 zoom"
 *
 * v17 核心范式转变：
 *   "保留最优路径" → "统计意义上的最优策略"
 *
 * 与 v16 的根本区别：
 *   v16: deterministic argmax beam search
 *   v17: stochastic tree search with UCT
 *
 * MCTS-UCT 组件对应关系：
 *   Component       | v17 实现
 *   Policy          | decideTransition() — constrained action space
 *   Selection       | UCT selection — balance explore/exploit
 *   Expansion       | expand() — add all valid child nodes
 *   Simulation      | stochasticRollout() — Monte Carlo evaluation
 *   Backpropagation | backpropagate() — update visits + Q-values
 *   Value function  | evaluateFullSequence() — 直接复用 v16
 **/
export function beamSearchTransitionPlan(
  shots: Shot[],
  emotions: number[],
  fps: number
): TransitionPlan {
  const energyCurve = buildGlobalEnergyCurve(shots, emotions, fps);

  // ── Step 1: 构建根节点 ────────────────────────────────────
  // 根节点代表"尚未做任何决策"的初始状态
  const rootAbstractKey = getStateAbstractionKey(
    0.5, 0.5, 0, "zoom", 0, 0
  );
  const root: MctsNode = {
    shotIndex: -1,
    abstractionKey: rootAbstractKey,
    state: { budget: MAX_BUDGET, cooldown: 0, lastTransition: "zoom" as TransitionType },
    parent: null,
    children: [],
    visits: 0,
    Q: 0,
    consecutiveWhip: 0,
    type: null,
    plan: new Map(),
  };

  // v18: 重置 Reward Data Collector（新 episode）
  rewardCollector.resetEpisode();
  // v19.6b: reset Q-table — old Q values from previous reward function cause
  //   corr(Q,reward) = -0.95 even with correct rollout + reward aligned.
  //   Q must be relearned under the new reward from scratch each episode.
  resetGlobalQTable();

  // ── Step 2: MCTS-UCT 主循环 ───────────────────────────────
  // BETA=0.3: visit bonus regularizes Q-noise; β=0.1 made separability worse.
  const SIMULATION_COUNT = 5;  // v20 fix: 3→5, noisy Q requires more rollouts for stability
  const EXPLORATION_CONSTANT = 1.0;  // v20 fix: 2.0→1.0, reduce noise-driven exploration
  // STRUCTURE_BONUS: v20 exploration fix — penalize repeated types to force structural diversity
  // When backtrackPlan → all-zoom tail collapses Q values, UCT near-random collapses to whip/fade-first.
  // Adding type-count penalty drives MCTS to explore under-represented structures (all-fade, alternating).
  const STRUCTURE_BONUS = 0.15;

  for (let i = 0; i < shots.length - 1; i++) {
    let currentNode = root;

    // ── Selection：从根向下 UCT 选择到当前层 ────────────────
    for (let depth = 0; depth <= i; depth++) {
      if (currentNode.children.length === 0) break;

      const N_parent = currentNode.visits;

      // Count transition types in ancestor path (for diversity penalty)
      const typeCounts: Record<TransitionType, number> = { whip: 0, zoom: 0, fade: 0 };
      let cur2: MctsNode | null = currentNode;
      while (cur2 !== null && cur2.type !== null) {
        typeCounts[cur2.type] = (typeCounts[cur2.type] || 0) + 1;
        cur2 = cur2.parent;
      }

      let bestChild = currentNode.children[0];
      let bestUCT = -Infinity;

      for (const child of currentNode.children) {
        if (child.visits === 0) {
          bestUCT = Infinity;
          bestChild = child;
          break;
        }
        // v19.6b fix: use node-local Q (updated via backpropagate from actual rollouts)
        // instead of globalQTable abstraction — which collapsed all plans into the same
        // abstract state (6-bucket discretization) making Q a visitation frequency counter,
        // not a plan quality estimator. Node-local Q properly tracks per-plan reward.
        const exploitation = child.Q;
        const exploration = EXPLORATION_CONSTANT * Math.sqrt(Math.log(N_parent) / child.visits);
        // v20 structure diversity bonus: penalize transition types already frequent in this path.
        // Each shot contributes 1 to its type count; max count = depth (all same type = max penalty).
        // Normalized: divide by (depth+1) so penalty is per-step and doesn't grow with tree depth.
        const childTypeCount = child.type ? (typeCounts[child.type] || 0) : 0;
        const structPenalty = STRUCTURE_BONUS * (childTypeCount / (depth + 1));
        const uct = exploitation + exploration - structPenalty;
        if (uct > bestUCT) {
          bestUCT = uct;
          bestChild = child;
        }
      }

      currentNode = bestChild;
    }

    // currentNode 现在是第 i 层的最佳选择节点
    if (currentNode.children.length === 0) {
      // ── Expansion：为此 shot 展开所有合法 action ─────────
      const childShotIndex = currentNode.shotIndex + 1;
      const emotion = emotions[childShotIndex] ?? 0.5;
      const beat = Math.sin((shots[childShotIndex].start / fps) * 0.05);
      const energy = energyCurve.find(
        (p) => p.frame >= shots[childShotIndex].start && p.frame < shots[childShotIndex].start + shots[childShotIndex].duration
      )?.energy ?? emotion;

      for (const ttype of TRANSITION_TYPES) {
        const { type: legalType, newState, newConsecutiveWhip } = decideTransition(
          ttype, currentNode.state, emotion, beat, currentNode.consecutiveWhip
        );

        const childPlan = new Map(currentNode.plan);
        childPlan.set(currentNode.shotIndex, { shotIndex: currentNode.shotIndex, type: legalType });

        // v17.1: 生成抽象状态键，使相似 state 共享 Q-value
        const childAbstractKey = getStateAbstractionKey(
          energy, emotion, shots[childShotIndex].start,
          newState.lastTransition, newState.cooldown, newConsecutiveWhip
        );

        const childNode: MctsNode = {
          shotIndex: childShotIndex,
          abstractionKey: childAbstractKey,
          state: newState,
          parent: currentNode,
          children: [],
          visits: 0,
          Q: 0,
          consecutiveWhip: newConsecutiveWhip,
          type: legalType,
          plan: childPlan,
        };

        currentNode.children.push(childNode);
      }
    }

    // ── Simulation：对当前 shot 层所有子节点做 deterministic evaluation ──
    // v19.6b fix: 关键结构性修复
    // OLD: stochasticRollout → backpropagate rollout reward
    //   问题: rollout 生成随机补全 plan，与 actual backtrack plan reward 结构性 mismatch
    //   → corr(Q,reward) = -0.98 (self-reinforced wrong policy basin)
    // NEW: backtrackPlan → evaluate → backpropagate actual plan reward
    //   直接消除 rollout-reward vs actual-reward 的 distribution gap
    for (const childNode of currentNode.children) {
      const childBacktrackPlan = backtrackPlan(childNode, shots);
      postProcessPlan(childBacktrackPlan, shots, emotions, energyCurve, fps);
      const { score: backtrackReward } = computeRewardFeatures(
        childBacktrackPlan, energyCurve, shots, emotions, fps
      );
      backpropagate(childNode, backtrackReward);
    }
  }

  // ── Step 3: 从根节点 children 中选最优 action ───────────────
  // v19.6b fix: softmax over Q + exploration bonus (not raw visits)
  //   logit = α*Q + β*ln(visits+1)
  //   α=1: reward estimate is primary signal
  //   β=0.3: visit bonus regularizes Q-noise from 3-rollout simulations.
  //     Increasing rollouts made separability WORSE (Q-accurate but biased vs real reward).
  //     Noisier Q paradoxically helps: lets alternatives occasionally win.
  //   This gives calibrated preference distribution, not frequency distribution.
  function softmaxSample(nodes: MctsNode[], temperature: number = 1.0): MctsNode {
    if (nodes.length === 0) return null as any;
    if (nodes.length === 1) return nodes[0];

    // ── Q normalization (critical for calibrated logits) ────────────────
    // Q ∈ [0,1] (sigmoid output) but log(visits+1) ∈ [0,~6]
    // Without normalization: visit term dominates → soft greedy
    // Fix: min-max normalize Q within root children to [0, 1]
    const qValues = nodes.map(n => n.Q);
    const qMin = Math.min(...qValues);
    const qMax = Math.max(...qValues);
    const qRange = qMax - qMin || 1;  // avoid div-by-zero for single-child nodes

    const ALPHA = 1.0;
    const BETA = 0.3;
    const logits = nodes.map((n, i) => {
      // Q_norm ∈ [0,1]: reward quality relative to sibling alternatives
      // visits bonus: unexplored nodes get slight uplift (controlled exploration)
      const qNorm = (n.Q - qMin) / qRange;
      return ALPHA * qNorm + BETA * Math.log(n.visits + 1);
    });
    const maxLogit = Math.max(...logits);

    const weights = logits.map(v => Math.exp((v - maxLogit) / temperature));
    const total = weights.reduce((a, b) => a + b, 0);
    const probs = weights.map(w => w / total);

    let r = Math.random() * (1 - 1e-9);
    for (let i = 0; i < nodes.length; i++) {
      r -= probs[i];
      if (r <= 0) return nodes[i];
    }
    return nodes[nodes.length - 1];
  }

  // 用 softmax 采样选择最优 action（非 greedy）
  // v20: temperature=2.5 — with structure diversity bonus, children Q values may be
  // compressed (similar tails), so higher temperature needed to maintain diversity exploration.
  const bestRootChild = softmaxSample(root.children, 2.5);

  // v20 FIX: argmax(reward) replaces softmax(Q) — Q is noisy, use real reward for selection
  if (root.children.length === 0) {
    return fallbackGreedyPlan(shots, emotions, fps, energyCurve);
  }

  // Evaluate ALL root children with pure rule reward, pick the best
  // Model score is computed for logging only — NOT used in argmax (avoids distribution shift)
  const childRewards = [];
  for (const child of root.children) {
    const childPlan = backtrackPlan(child, shots);
    postProcessPlan(childPlan, shots, emotions, energyCurve, fps);
    const { features: childFeatures, score: childRuleScore } = computeRewardFeatures(childPlan, energyCurve, shots, emotions, fps);
    // Build context for model logging (matching dataset_loader._build_features)
    const shot_count = shots.length;
    const duration_frames = shots.reduce((s, sh) => s + sh.duration, 0);
    const emotionHist = new Array(10).fill(0);
    for (const e of emotions) { const bucket = Math.min(9, Math.floor(e * 10)); emotionHist[bucket]++; }
    const energyHist = new Array(10).fill(0);
    for (let si = 0; si < shots.length; si++) { const bucket = Math.min(9, Math.floor((emotions[si] ?? 0.5) * 10)); energyHist[bucket]++; }
    const ctx = { shot_count, fps, duration_frames, emotion_histogram: emotionHist, energy_histogram: energyHist };
    const x = buildFeatureVector(childFeatures as RewardFeatures, ctx);
    const rModel = predictInline(x);
    childRewards.push({ child, plan: childPlan, score: childRuleScore, modelScore: rModel });
  }

  // argmax(reward) — NOT softmax(Q)
  let bestChild = childRewards[0].child;
  let bestPlan = childRewards[0].plan;
  let bestScore = childRewards[0].score;
  for (const cr of childRewards) {
    if (cr.score > bestScore) {
      bestScore = cr.score;
      bestChild = cr.child;
      bestPlan = cr.plan;
    }
  }

  const { features: selectedFeatures, score: selectedScore } = computeRewardFeatures(
    bestPlan, energyCurve, shots, emotions, fps
  );

  const alternatives = [];
  for (const cr of childRewards) {
    if (cr.child === bestChild) continue;
    alternatives.push({
      plan: Array.from(cr.plan.entries()).map(([shot, dec]) => ({
        shot,
        type: dec.type,
        microCutAt: dec.microCutAt,
        microCutIntensity: dec.microCutIntensity,
      })),
      reward: cr.score,
      features: (() => {
        const f = computeRewardFeatures(cr.plan, energyCurve, shots, emotions, fps);
        return f.features;
      })(),
      qValue: cr.child.Q,
      visits: cr.child.visits,
    });
  }

  const nonBest = childRewards.filter(cr => cr.child !== bestChild);
  if (nonBest.length > 0) {
    const sampled = softmaxSample(nonBest.map(cr => cr.child), 2.5);
    if (sampled) {
      const sampledCR = nonBest.find(cr => cr.child === sampled);
      if (sampledCR) {
        alternatives.push({
          plan: Array.from(sampledCR.plan.entries()).map(([shot, dec]) => ({
            shot,
            type: dec.type,
            microCutAt: dec.microCutAt,
            microCutIntensity: dec.microCutIntensity,
          })),
          reward: sampledCR.score,
          features: (() => {
            const f = computeRewardFeatures(sampledCR.plan, energyCurve, shots, emotions, fps);
            return f.features;
          })(),
          qValue: sampled.Q,
          visits: sampled.visits,
        });
      }
    }
  }

// ── v19.6b: Add hard negative plans（结构对比样本）───────────────────
  // These are intentionally extreme plans that differ structurally from selected.
  // They provide REAL ranking signal unlike MCTS siblings which are nearly identical.
  const hardNegatives = [
    { name: "all-zoom", plan: genAllZoomPlan(shots) },
    { name: "all-fade", plan: genAllFadePlan(shots) },
    { name: "anti-energy", plan: genAntiEnergyPlan(shots, emotions, energyCurve, fps) },
    { name: "alternating", plan: genAlternatingPlan(shots) },
  ];
  for (const hn of hardNegatives) {
    postProcessPlan(hn.plan, shots, emotions, energyCurve, fps);
    const hnResult = computeRewardFeatures(hn.plan, energyCurve, shots, emotions, fps);
    alternatives.push({
      name: hn.name,  // "all-zoom", "all-fade", "anti-energy", "alternating"
      plan: Array.from(hn.plan.entries()).map(([shot, dec]) => ({
        shot,
        type: dec.type,
        microCutAt: dec.microCutAt,
        microCutIntensity: dec.microCutIntensity,
      })),
      reward: hnResult.score,
      features: hnResult.features,
      qValue: 0,  // synthetic plan has no MCTS Q
      visits: 0,
    });
  }

  rewardCollector.collect(
    bestPlan, selectedScore, selectedFeatures,
    alternatives, shots, emotions, fps
  );
  rewardCollector.setMctsStats(root);

  return bestPlan;
}

/**
 * v19.6b: Reward-Aligned Stochastic Rollout
 *
 * CRITICAL: rollout policy MUST align with reward function semantics.
 *
 * Old mismatch (caused corr=-0.95):
 *   rollout threshold: energy >= 0.75 → whip
 *   reward threshold:  energy >= 0.65 → whip +1.0 / fade -0.5
 *   → rollout wasted [0.65, 0.75) on zoom while reward wanted whip
 *
 * New strategy: energy threshold matches reward (0.65).
 *   energy >= 0.65: whip is rewarded → pick it reliably
 *   energy <  0.65: whip is penalized, fade has small positive → prefer fade
 *   Small noise still added to avoid deterministic collapse.
 **/
function stochasticRollout(
  startNode: MctsNode,
  shots: Shot[],
  emotions: number[],
  energyCurve: Array<{ frame: number; energy: number }>,
  fps: number
): number {
  const plan = new Map(startNode.plan);
  let state = { ...startNode.state };
  let consecutiveWhip = startNode.consecutiveWhip;

  for (let i = startNode.shotIndex + 1; i < shots.length - 1; i++) {
    const emotion = emotions[i] ?? 0.5;
    const energy = energyCurve.find(
      (p) => p.frame >= shots[i].start && p.frame < shots[i].start + shots[i].duration
    )?.energy ?? emotion;

    const beat = Math.sin((shots[i].start / fps) * 0.05);

    // ── v19.6b: Reward-aligned stochastic action selection ────────
    // Matches evaluateFullSequence energy threshold (0.65).
    //   energy >= 0.65: whip → reward +1.0, fade → -0.5  → pick whip
    //   energy <  0.65: whip → -0.5,  fade → +0.2, zoom → 0  → pick fade
    const rand = Math.random();
    const isHighEnergy = energy >= 0.65;  // matches reward threshold

    let chosenType: TransitionType;
    if (isHighEnergy) {
      // reward-aligned: whip is best in high energy
      chosenType = rand < 0.85 ? "whip" : (rand < 0.95 ? "fade" : "zoom");
    } else {
      // reward-aligned: whip is bad, fade is best (small positive)
      chosenType = rand < 0.70 ? "fade" : (rand < 0.90 ? "zoom" : "whip");
    }

    const { type: legalType, newState, newConsecutiveWhip } = decideTransition(
      chosenType, state, emotion, beat, consecutiveWhip
    );

    plan.set(i, { shotIndex: i, type: legalType });
    state = newState;
    consecutiveWhip = newConsecutiveWhip;
  }

  return evaluateFullSequence(plan, energyCurve, shots, emotions, fps);
}

/**
 * v17: Backpropagation（更新 visit count + Q-value）
 *
 * MCTS 的核心：通过 backpropagation 累积统计量
 * 使得高频访问节点的 Q 值趋于稳定
 *
 * Q-value 更新公式（增量平均）：
 *   Q_new = Q_old + (R - Q_old) / visits
 *   其中 R = evaluateFullSequence(full_plan)
 **/
function backpropagate(node: MctsNode | null, reward: number): void {
  while (node !== null) {
    node.visits++;
    node.Q = node.Q + (reward - node.Q) / node.visits;
    // v17.1: 同时更新全局 Q-Table（跨节点共享）
    updateAbstractQ(node.abstractionKey, reward);
    node = node.parent;
  }
}

/**
 * v17: UCT Fallback Greedy（当 MCTS 未能展开时退保）
 **/
function fallbackGreedyPlan(
  shots: Shot[],
  emotions: number[],
  fps: number,
  energyCurve: Array<{ frame: number; energy: number }>
): TransitionPlan {
  const plan = new Map<number, TransitionDecision>();
  let state = { budget: MAX_BUDGET, cooldown: 0, lastTransition: "zoom" as TransitionType };
  let consecutiveWhip = 0;

  for (let i = 0; i < shots.length - 1; i++) {
    const emotion = emotions[i] ?? 0.5;
    const beat = Math.sin((shots[i].start / fps) * 0.05);

    let chosenType: TransitionType = "zoom";
    const { type: legalType, newState, newConsecutiveWhip } = decideTransition(
      chosenType, state, emotion, beat, consecutiveWhip
    );

    plan.set(i, { shotIndex: i, type: legalType });
    state = newState;
    consecutiveWhip = newConsecutiveWhip;
  }

  return plan;
}

// ── v19.6b: Hard Negative Generators（对比样本构造）────────────────
// These create structurally extreme plans that provide real ranking signal.
// Unlike MCTS siblings (which are very similar), these vary by composition.

function findShotEnergy(shotIdx: number, shots: Shot[], energyCurve: Array<{ frame: number; energy: number }>, emotions: number[], fps: number): number {
  const shot = shots[shotIdx];
  if (!shot) return 0.5;
  const peakFrame = findEmotionPeakFrame(shot, energyCurve, 0.6, fps);
  return energyCurve.find(p => Math.abs(p.frame - peakFrame) < fps * 0.5)?.energy ?? emotions[shotIdx] ?? 0.5;
}

/** 1. All-zoom plan: minimum transition diversity baseline */
function genAllZoomPlan(shots: Shot[]): TransitionPlan {
  const plan = new Map<number, TransitionDecision>();
  for (let i = 0; i < shots.length - 1; i++) {
    plan.set(i, { shotIndex: i, type: "zoom" });
  }
  return plan;
}

/** 2. All-fade plan: another monotonic baseline */
function genAllFadePlan(shots: Shot[]): TransitionPlan {
  const plan = new Map<number, TransitionDecision>();
  for (let i = 0; i < shots.length - 1; i++) {
    plan.set(i, { shotIndex: i, type: "fade" });
  }
  return plan;
}

/** 3. Anti-energy plan: whip at LOW energy, fade at HIGH energy — intentionally wrong alignment */
function genAntiEnergyPlan(
  shots: Shot[],
  emotions: number[],
  energyCurve: Array<{ frame: number; energy: number }>,
  fps: number
): TransitionPlan {
  const plan = new Map<number, TransitionDecision>();
  for (let i = 0; i < shots.length - 1; i++) {
    const e = findShotEnergy(i, shots, energyCurve, emotions, fps);
    // Intentional anti-alignment: whip when low energy, fade when high
    plan.set(i, { shotIndex: i, type: e < 0.5 ? "fade" : "whip" });
  }
  return plan;
}

/** 4. Alternating plan: whip→zoom→fade→whip cycling — maximum type variation */
function genAlternatingPlan(shots: Shot[]): TransitionPlan {
  const CYCLE: TransitionType[] = ["whip", "zoom", "fade"];
  const plan = new Map<number, TransitionDecision>();
  for (let i = 0; i < shots.length - 1; i++) {
    plan.set(i, { shotIndex: i, type: CYCLE[i % 3] });
  }
  return plan;
}

/**
 * v14: 一次性规划整条视频的 transition 决策（整合版）
 *
 * @param shots - 镜头序列
 * @param emotions - 每个 shot 对应的情绪强度（0~1），长度应与 shots 一致
 * @param fps - 帧率
 *
 * v14 升级：
 *   - microCut 由能量峰值语义锚定（不再用固定 0.60）
 *   - 构建完成后调用 enforceWhipDensityConstraint（全局 whip 密度约束）
 *   - 返回 plan 附带全局评分（用于调试和未来优化）
 */

/**
 * v15: 统一入口（代理到 Beam Search 实现）
 *
 * buildTransitionPlan 保持 API 不变，内部替换为 beamSearchTransitionPlan
 * 使 v13 的 useTransitionPlan 无需改动
 */
function buildTransitionPlan(
  shots: Shot[],
  emotions: number[],
  fps: number
): TransitionPlan {
  // v15: 全局最优搜索（beam search）
  return beamSearchTransitionPlan(shots, emotions, fps);
}

/**
 * v13: 将 buildTransitionPlan 接入 React 渲染管线
 * 依赖 shots + emotions（都加了 useMemo），结果稳定不变
 */
function useTransitionPlan(
  shots: VideoLayout["shots"],
  emotions: number[],
  fps: number
): TransitionPlan {
  const plan = useMemo(
    () => buildTransitionPlan(shots ?? [], emotions, fps),
    [shots, emotions.join(","), fps]
  );
  return plan;
}

// ============================================================
// 动画计算 Hook
// ============================================================

function useElementAnimation(start: number, duration: number, animation?: VideoElement["animation"]) {
  const frame = useCurrentFrame();
  const t = Math.max(0, frame - start);
  const end = duration;
  const animDuration = animation?.duration ?? 15;

  // 入场进度 [0, animDuration]
  const enterProgress = Math.min(t / animDuration, 1);
  // 退场进度 [end-animDuration, end]
  const exitT = Math.max(0, t - (end - animDuration));
  const exitProgress = Math.min(exitT / animDuration, 1);

  // 基础 opacity
  let opacity = 1;
  let transform = "";

  if (t === 0) {
    opacity = 0;
  } else if (t < animDuration) {
    // 入场动画
    opacity = enterProgress;
  } else if (t > end - animDuration) {
    // 退场动画
    opacity = 1 - exitProgress;
  }

  // 入场 transform
  if (t < animDuration) {
    const enterType = animation?.enter ?? "fade";
    switch (enterType) {
      case "slide-up":
        transform = `translateY(${(1 - enterProgress) * 40}px)`;
        break;
      case "slide-down":
        transform = `translateY(${(1 - enterProgress) * -40}px)`;
        break;
      case "zoom-in":
        transform = `scale(${0.5 + enterProgress * 0.5})`;
        break;
      case "zoom-out":
        transform = `scale(${1.5 - enterProgress * 0.5})`;
        break;
      case "bounce-in": {
        const spring = enterProgress < 0.6
          ? 1.2
          : 1.05 - (enterProgress - 0.6) / 0.4 * 0.05;
        transform = `scale(${enterProgress * spring})`;
        break;
      }
      case "blur-in":
        opacity = enterProgress;
        transform = `blur(${(1 - enterProgress) * 8}px)`;
        break;
      default: // fade
        break;
    }
  } else {
    // 持续期间：轻微呼吸
    const breathe = 1 + Math.sin(t * 0.03) * 0.015;
    transform = `scale(${breathe})`;
  }

  // 退场 transform
  if (t > end - animDuration && exitProgress > 0) {
    const exitType = animation?.exit ?? "fade";
    switch (exitType) {
      case "slide-up":
        transform = `translateY(${-exitProgress * 40}px)`;
        break;
      case "slide-down":
        transform = `translateY(${exitProgress * 40}px)`;
        break;
      case "zoom-out":
        transform = `scale(${1 - exitProgress * 0.5})`;
        break;
      case "blur-out":
        transform = `blur(${exitProgress * 8}px)`;
        break;
      default: // fade
        break;
    }
    opacity = 1 - exitProgress;
  }

  return { opacity: Math.max(0, Math.min(1, opacity)), transform, isVisible: t >= 0 && t <= end };
}

// ============================================================
// 逐词高亮组件（className 固定字符串，零 React style diff）
// ============================================================

/**
 * WordHighlightedText — className 驱动高亮
 *
 * 性能策略：
 *   - isActive 最多每秒变几次 → className 字符串稳定
 *   - React diff className = 字符串比较（O(1)）
 *   - 无 style 对象创建，无 CSS 对象 diff
 *   - span 在 isActive 不变时完全跳过更新（React.memo）
 */
const WordHighlightedText: React.FC<{
  wordCues: Array<{ index: number; word: string; start: number; end: number }>;
  activeIndex: number;
  color: string;
  fontWeight?: number;
}> = React.memo(({ wordCues, activeIndex, color, fontWeight }) => {
  const fontWeightVal = fontWeight ?? 600;

  const spans = useMemo(() => {
    return wordCues.map((wc) => {
      const isActive = wc.index === activeIndex;
      // 两个 className 都是稳定字符串引用
      return (
        <span
          key={wc.index}
          className={isActive ? "word-active" : "word-inactive"}
          style={{
            color: isActive ? "#FFD700" : color,
            textShadow: isActive ? "0 0 12px #FFD700, 0 2px 8px rgba(0,0,0,0.9)" : "0 2px 12px rgba(0,0,0,0.8)",
            transition: "color 0.08s ease, text-shadow 0.08s ease",
            fontWeight: isActive ? 900 : fontWeightVal,
          }}
        >
          {wc.word}
        </span>
      );
    });
  }, [wordCues, activeIndex, color, fontWeightVal]);

  return <span style={{ fontFamily: FONT_FAMILY }}>{spans}</span>;
});
WordHighlightedText.displayName = "WordHighlightedText";

const TextLayer: React.FC<{ element: TextElement; frame: number }> = ({ element, frame }) => {
  const { opacity, transform, isVisible } = useElementAnimation(element.start, element.duration, element.animation);
  if (!isVisible) return null;

  const { fps } = useVideoConfig();
  const t = frame / fps;

  // ── 逐词高亮渲染 ─────────────────────────────────────────
  // 只在 wordCues 存在时才启用词级渲染
  if (element.wordCues && element.wordCues.length > 0) {
    // 找当前帧对应的词（区间相交判断）
    const activeWord = element.wordCues.find(
      (w) => t >= w.start && t <= w.end
    );
    if (activeWord) {
      // 渲染整句，active 词高亮
      return (
        <div
          style={{
            position: "absolute",
            left: element.x,
            top: element.y,
            fontFamily: FONT_FAMILY,
            fontSize: element.fontSize,
            fontWeight: element.fontWeight ?? 600,
            color: element.color,
            textAlign: (element.textAlign as "center") ?? "center",
            lineHeight: element.lineHeight ?? 1.3,
            maxWidth: element.maxWidth,
            opacity,
            transform,
            textShadow: `0 2px 12px rgba(0,0,0,0.8)`,
            zIndex: element.zIndex,
          }}
        >
          {/* 整句渲染，当前词高亮 */}
          <WordHighlightedText wordCues={element.wordCues} activeIndex={activeWord.index} color={element.color} fontWeight={element.fontWeight} />
        </div>
      );
    }
  }

  // fallback：普通整句渲染
  return (
    <div
      style={{
        position: "absolute",
        left: element.x,
        top: element.y,
        fontFamily: FONT_FAMILY,
        fontSize: element.fontSize,
        fontWeight: element.fontWeight ?? 600,
        color: element.color,
        textAlign: (element.textAlign as "center") ?? "center",
        lineHeight: element.lineHeight ?? 1.3,
        maxWidth: element.maxWidth,
        opacity,
        transform,
        textShadow: `0 2px 12px rgba(0,0,0,0.8)`,
        zIndex: element.zIndex,
      }}
    >
      {element.text}
    </div>
  );
};

const ImageLayerEl: React.FC<{ element: ImageElement }> = ({ element }) => {
  const { opacity, transform, isVisible } = useElementAnimation(element.start, element.duration, element.animation);
  if (!isVisible) return null;

  return (
    <div
      style={{
        position: "absolute",
        left: element.x,
        top: element.y,
        width: element.width,
        height: element.height,
        borderRadius: element.borderRadius ?? 12,
        overflow: "hidden",
        opacity,
        transform,
        boxShadow: `0 8px 32px rgba(0,0,0,0.5)`,
        zIndex: element.zIndex,
      }}
    >
      <Img
        src={element.src}
        style={{
          width: "100%",
          height: "100%",
          objectFit: (element.objectFit as "cover") ?? "cover",
        }}
      />
    </div>
  );
};

const StickerLayer: React.FC<{ element: StickerElement }> = ({ element }) => {
  const { opacity, transform, isVisible } = useElementAnimation(element.start, element.duration, element.animation);
  if (!isVisible) return null;

  return (
    <div
      style={{
        position: "absolute",
        left: element.x,
        top: element.y,
        fontSize: element.size,
        opacity,
        transform,
        filter: `drop-shadow(0 4px 12px rgba(0,0,0,0.4))`,
        zIndex: element.zIndex,
      }}
    >
      {element.emoji}
    </div>
  );
};

const ShapeLayer: React.FC<{ element: ShapeElement }> = ({ element }) => {
  const { opacity, transform, isVisible } = useElementAnimation(element.start, element.duration, element.animation);
  if (!isVisible) return null;

  const baseStyle = {
    position: "absolute" as const,
    left: element.x,
    top: element.y,
    width: element.shape === "line" ? element.width : element.width,
    height: element.shape === "line" ? 2 : element.height,
    backgroundColor: element.shape === "line" ? element.color : (element.fillColor ?? "transparent"),
    border: element.shape !== "line" ? `2px solid ${element.color}` : undefined,
    borderRadius: element.shape === "circle"
      ? "50%"
      : element.borderRadius ?? 8,
    opacity,
    transform: element.rotation ? `${transform} rotate(${element.rotation}deg)` : transform,
    zIndex: element.zIndex,
  };

  return <div style={baseStyle} />;
};

const BackgroundLayer: React.FC<{ element: BackgroundElement; frame: number }> = ({ element, frame }) => {
  const { opacity, transform, isVisible } = useElementAnimation(element.start, element.duration, element.animation);
  if (!isVisible) return null;

  // 背景轻微呼吸
  const bgShift = Math.sin(frame * 0.008) * 10;

  return (
    <div
      style={{
        position: "absolute",
        top: 0,
        left: 0,
        right: 0,
        bottom: 0,
        background: element.gradient ?? element.color ?? "#0A0E14",
        opacity,
        transform,
        backgroundSize: "200% 200%",
        zIndex: element.zIndex,
      }}
    />
  );
};

// ============================================================
// 导演状态 Hook
// ============================================================

/**
 * 每帧从导演意图查询当前状态
 *
 * 全局唯一的动态状态来源：
 * evaluateDirector(director, t, duration) 每帧返回：
 * - emotion:      情绪强度（0~1）
 * - pacing:       节奏速度倍率
 * - visualFocus:  视觉聚焦程度
 * - audioIntensity: 音频强度
 * - scene:        当前 scene 类型
 *
 * 如果 layout.director 不存在（兼容旧代码），返回零值
 */
function useDirectorState(layout: VideoLayout): DirectorState | null {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  const t = frame / fps;
  const duration = durationInFrames / fps;

  if (!layout.director) return null;
  return evaluateDirector(layout.director, t, duration);
}

// ============================================================
// 镜头系统（v10：shot 驱动相机）
// ============================================================

/**
 * 当前帧对应的镜头（shot）
 * shots 是一个时间轴序列，按 start 排序
 * 找不到时返回 undefined（layout.shots 不存在时也走这里）
 */
/**
 * v10.3: 镜头切换连续性（Temporal Continuity）
 *
 * 返回当前 shot + 下一 shot + 切换进度
 * 当 frame 进入 shot 末尾 TRANSITION_FRAMES 时：
 *   current 渐出（cross-zoom + direction exit）
 *   next    渐入（direction enter）
 *
 * 效果：从"硬切" → "像同一个镜头延续"
 */
const TRANSITION_FRAMES = 8;

/**
 * v13: 执行层 — 查规划 + 驱动 CSS transform
 *
 * 相比 v12 的根本区别：
 *   v12: 每帧 reactive 决策（module state → 跨视频污染风险）
 *   v13: 查预建规划（pure lookup → 无状态，无污染）
 *
 * microCut: shot 内部 micro-cut（v13 新增）
 *   在 shot 的 microCutAt 时刻注入微冲击
 *   效果：镜头内部也有剪辑感（不只是 shot boundary）
 */
function useShotsAroundFrame(
  shots: VideoLayout["shots"],
  cameraOverride: string,
  plan: TransitionPlan,
  emotions: number[]
): {
  current: Shot | null;
  next: Shot | null;
  currentTransform: string;
  nextTransform: string;
  nextShotTransform: string;
  nextEmotionTransform: string;
  currentOpacity: number;
  nextOpacity: number;
  isTransitioning: boolean;
} {
  const frame = useCurrentFrame();
  if (!shots || shots.length === 0) {
    return { current: null, next: null, currentTransform: "", nextTransform: "", nextShotTransform: "", nextEmotionTransform: "", currentOpacity: 1, nextOpacity: 0, isTransitioning: false as boolean };
  }

  const idx = shots.findIndex((s) => frame >= s.start && frame < s.start + s.duration);
  const current = idx >= 0 ? shots[idx] : null;
  const next = idx >= 0 && idx + 1 < shots.length ? shots[idx + 1] : null;

  if (!current) {
    return { current: null, next: null, currentTransform: "", nextTransform: "", nextShotTransform: "", nextEmotionTransform: "", currentOpacity: 1, nextOpacity: 0, isTransitioning: false };
  }

  const shotEnd = current.start + current.duration;
  const inWindow = !!(frame >= shotEnd - TRANSITION_FRAMES && frame < shotEnd && next);
  const t = inWindow ? (frame - (shotEnd - TRANSITION_FRAMES)) / TRANSITION_FRAMES : 0;
  const isTransitioning = !!(inWindow && t >= 0 && t <= 1);

  // ── v13: 从预建规划中查 TransitionDecision ───────────────────
  const decision = plan.get(idx);
  const transitionType: TransitionType = decision?.type ?? "zoom";

  // ── v13: Shot 内部 micro-cut ────────────────────────────────
  // 在 microCutAt 时刻注入 scale spike（制造 shot 内部剪辑感）
  const progressInShot = current.duration > 0 ? (frame - current.start) / current.duration : 0;
  const microCutAt = decision?.microCutAt ?? 0.60;
  const microCutIntensity = decision?.microCutIntensity ?? 0.08;
  const nearMicroCut = Math.abs(progressInShot - microCutAt) < 0.025;
  const microCutScale = nearMicroCut ? 1 + microCutIntensity : 1;

  // ── v10.5: Impact frame — 中间帧微冲击（制造"剪辑点"节奏感）──
  // 仅 zoom 类型生效，在 t≈0.5 时产生一个 scale spike
  const isImpact = transitionType === "zoom" && isTransitioning && Math.abs(t - 0.5) < 0.12;
  const impactScale = isImpact ? 1.08 : 1;

  // ── v10.4: Direction-aware pan continuity ──────────────────
  // current exit direction
  let exitTranslate = 0;
  if (current.camera === "pan-left") {
    exitTranslate = -t * 120;
  } else if (current.camera === "pan-right") {
    exitTranslate = t * 120;
  }
  // next enter direction（与 exit 相反，形成视觉连续）
  let enterTranslate = 0;
  if (next) {
    if (next.camera === "pan-left") {
      enterTranslate = (1 - t) * 120;
    } else if (next.camera === "pan-right") {
      enterTranslate = -(1 - t) * 120;
    }
  }

  // ── v11: transition type 决定具体数值 ─────────────────────
  let currentTransform = "";
  let nextTransform = "";
  let currentOpacity = 1;
  let nextOpacity = 0;

  if (transitionType === "whip") {
    // whip pan：横向甩切（t=0→1，current快速右甩出，next从右滑入）
    const whipCurrent = isTransitioning ? interpolate(t, [0, 1], [0, 800], { easing: Easing.out(Easing.quad) }) : 0;
    const whipNext = isTransitioning ? interpolate(t, [0, 1], [200, 0], { easing: Easing.out(Easing.quad) }) : 0;
    currentTransform = `translateX(${whipCurrent}px) scale(${microCutScale})`;
    nextTransform = `translateX(${whipNext}px)`;
    // whip: 透明度在最后一段才切（不是全程淡）
    const whipCutoff = interpolate(t, [0, 1], [0, 1], { easing: Easing.linear });
    currentOpacity = isTransitioning ? Math.max(0, 1 - whipCutoff * 1.8) : 1;
    nextOpacity = isTransitioning ? Math.min(1, (whipCutoff - 0.3) * 1.5) : 0;
    // 微噪声
    const noise = Math.sin(frame * 13.7) * 0.012;
    currentOpacity = Math.max(0, Math.min(1, currentOpacity + noise));
    nextOpacity = Math.max(0, Math.min(1, nextOpacity + Math.sin(frame * 11.3 + 1.5) * 0.012));
  } else if (transitionType === "fade") {
    // fade：纯 opacity 渐变，无 scale（适合慢内容）
    currentTransform = `translateX(${exitTranslate * 0.3}px) scale(${microCutScale})`;
    nextTransform = `translateX(${-enterTranslate * 0.3}px)`;
    const fadeT = isTransitioning ? interpolate(t, [0, 1], [1, 0], { easing: Easing.linear }) : 1;
    const fadeNext = isTransitioning ? interpolate(t, [0, 1], [0, 1], { easing: Easing.linear }) : 0;
    const noise = Math.sin(frame * 7.3) * 0.008;
    currentOpacity = Math.max(0, Math.min(1, fadeT + noise));
    nextOpacity = Math.max(0, Math.min(1, fadeNext + Math.sin(frame * 5.9 + 1.5) * 0.008));
  } else {
    // zoom（默认）：cross-zoom + impact frame + pan continuity
    const exitZoom = isTransitioning ? interpolate(t, [0, 1], [1, 1.2], { easing: Easing.in(Easing.quad) }) : 1;
    const exitFade = isTransitioning ? interpolate(t, [0, 1], [1, 0], { easing: Easing.linear }) : 1;
    const enterZoom = isTransitioning ? interpolate(t, [0, 1], [1.15, 1], { easing: Easing.out(Easing.quad) }) : 1;
    const enterFade = isTransitioning ? interpolate(t, [0, 1], [0, 1], { easing: Easing.linear }) : 0;
    // v13 microCutScale: 镜头内部微冲击（叠加在 exitZoom 之上）
    currentTransform = `translateX(${exitTranslate}px) scale(${exitZoom * impactScale * microCutScale})`;
    nextTransform = `translateX(${-enterTranslate}px) scale(${enterZoom})`;
    const noise = Math.sin(frame * 13.7) * 0.015;
    const nextNoise = Math.sin(frame * 11.3 + 1.5) * 0.015;
    currentOpacity = Math.max(0, Math.min(1, exitFade + noise));
    nextOpacity = Math.max(0, Math.min(1, enterFade + nextNoise));
  }

  // ── v10.4: next 也跑完整 camera pipeline（camera continuity）──
  // nextProgress = 0 表示"进入的第一帧应该是什么姿态"
  const nextProgress = 0;
  const {
    shotTransform: nextShotTransform,
    emotionTransform: nextEmotionTransform,
  } = next
    ? getShotTransform(next, nextProgress, cameraOverride, frame)
    : { shotTransform: "", emotionTransform: "" };

  return {
    current,
    next,
    currentTransform,
    nextTransform,
    nextShotTransform,
    nextEmotionTransform,
    currentOpacity,
    nextOpacity,
    isTransitioning,
  };
}

/**
 * 计算 shot 的 CSS transform（crop + camera motion）
 * cropX/cropY/cropW/cropH 是 0~1 相对值
 * camera 类型决定是否有额外的 scale/translate 动画
 */
function getShotTransform(
  shot: Shot,
  progress: number,
  cameraOverride: string,
  frame: number
): { shotTransform: string; emotionTransform: string } {
  const { cropX = 0, cropY = 0, cropW = 1, cropH = 1, camera } = shot;
  const W = 1080, H = 1920;

  // v10.2: progress clamp（防止边界帧越界导致 easing 抖动）
  const clamped = Math.max(0, Math.min(1, progress));

  // v10.2 bonus: emotion-aware easing（情绪决定运动曲线）
  const easingFn = cameraOverride === "shake"
    ? Easing.linear
    : cameraOverride === "pulse"
    ? Easing.inOut(Easing.quad)
    : Easing.inOut(Easing.cubic);

  const eased = interpolate(clamped, [0, 1], [0, 1], {
    easing: easingFn,
    extrapolateLeft: "clamp",
    extrapolateRight: "clamp",
  });

  // camera 起始/结束 crop（用于 interpolate 连续运动）
  // push-in: scale 1.0 → 1.2（放大 = 推近）
  // pan-left: cropX 0 → 0.15（裁剪右移 = 向左看）
  // pan-right: cropX 0.15 → 0（裁剪左移 = 向右看）
  // pull-out: scale 1.2 → 1.0（缩小 = 拉远）
  // static: 无变化
  let startCropW = 1, endCropW = 1;
  let startCropX = 0, endCropX = 0;
  let startScale = 1, endScale = 1;
  let startCropH = 1, endCropH = 1;

  if (camera === "push-in") {
    startScale = 1.0; endScale = 1.2;
    startCropH = 1; endCropH = 0.88;
  } else if (camera === "pan-left") {
    startCropX = 0; endCropX = 0.15; startCropW = 1; endCropW = 0.88;
  } else if (camera === "pan-right") {
    startCropX = 0.12; endCropX = 0; startCropW = 0.88; endCropW = 1;
  } else if (camera === "pull-out") {
    startScale = 1.15; endScale = 1.0;
    startCropH = 0.88; endCropH = 1;
  }

  // 按 easing 曲线插值（连续运动，非跳变）
  const curCropW = startCropW + (endCropW - startCropW) * eased;
  const curCropH = startCropH + (endCropH - startCropH) * eased;
  const curCropX = startCropX + (endCropX - startCropX) * eased;
  const curScale = startScale + (endScale - startScale) * eased;

  // crop → scale 变换（X/Y 同步，防止比例不自然）
  const scaleX = curScale / curCropW;
  const scaleY = curScale / curCropH; // v10.2 修复：X/Y 同步用变量 curCropH（之前错用固定 cropH）
  const baseTranslateX = -(curCropX * W) * scaleX;
  const translateY = -(cropY * H) * scaleY;

  // v10.2: 多频 drift（消除完全规则运动的假感，不同频率叠加 → 非周期感）
  const drift = camera !== "static"
    ? Math.sin(frame * 0.021) * 4 + Math.sin(frame * 0.013 + 1.7) * 2
    : 0;
  const translateX = baseTranslateX + drift;

  const shotTransform = `translate(${translateX}px, ${translateY}px) scale(${scaleX}, ${scaleY})`;

  // emotion camera 叠加（shake / pulse / slow-zoom），不是覆盖
  let emotionTransform = "";
  if (cameraOverride === "shake") {
    const jitter = 8;
    emotionTransform = `translate(${Math.sin(frame * 3.1) * jitter}px, ${Math.cos(frame * 2.7) * jitter}px)`;
  } else if (cameraOverride === "pulse") {
    const pulse = 1 + Math.sin(frame * 0.05) * 0.02;
    emotionTransform = `scale(${pulse})`;
  } else if (cameraOverride === "slow-zoom") {
    const slow = 1 + Math.sin(frame * 0.015) * 0.01;
    emotionTransform = `scale(${slow})`;
  }

  return { shotTransform, emotionTransform };
}

// ============================================================
// 场景渲染
// ============================================================

export const VideoScene: React.FC<{ layout: VideoLayout }> = ({ layout }) => {
  const frame = useCurrentFrame();
  const { width, height, background, elements } = layout;

  // 按 zIndex 排序
  const sortedElements = [...elements].sort((a, b) => (a.zIndex ?? 0) - (b.zIndex ?? 0));

  // 全局淡入
  const fadeIn = interpolate(frame, [0, 20], [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp" });

  // ========== 导演驱动的镜头系统 ==========
  // 由 useDirectorState 每帧从 evaluateDirector() 实时查询
  const directorState = useDirectorState(layout);

  // ── v13: 为每个 shot 计算代表情绪（用于 transition 规划）──
  // emotion 在 shot 中点采样，得到 per-shot 的情绪序列
  const { fps, durationInFrames } = useVideoConfig();
  const emotions = useMemo(() => {
    if (!layout.shots || !layout.director) return [];
    return layout.shots.map((shot) => {
      const midT = (shot.start + shot.duration / 2) / fps;
      const duration = durationInFrames / fps;
      const state = evaluateDirector(layout.director!, midT, duration);
      return state?.emotion ?? 0.5;
    });
  }, [layout.shots, layout.director, fps, durationInFrames]);

  // ── v13: Transition Planner（整条视频一次性规划）─────────────
  const transitionPlan = useTransitionPlan(layout.shots ?? [], emotions, fps);

  // ── 全局 zoom（开场冲击 → 情绪驱动）──
  // emotionEffect.zoomBase 由情绪层决定（intense=1.05, calm=1.0）
  const introZoom = directorState
    ? directorState.emotionEffect.zoomBase + directorState.emotion * 0.2
    : interpolate(frame, [0, 20, 40, 70], [1.2, 1.05, 1.08, 1], {
        extrapolateLeft: "clamp",
        extrapolateRight: "clamp",
      });

  // ── 情绪推进（随视频推进微微 zoom in）──
  const camPush = directorState
    ? 1 + directorState.pacing * 0.08
    : interpolate(frame, [0, 300], [1, 1.08], {
        extrapolateLeft: "clamp",
        extrapolateRight: "clamp",
      });

  // ── 呼吸脉动（情绪强度控制幅度）──
  // emotionEffect.breatheIntensity：intense=0.8，calm=0.3
  const breatheIntensity = directorState?.emotionEffect.breatheIntensity ?? 0.4;
  const breathe = 1 + Math.sin(frame * 0.025) * 0.006 * breatheIntensity;

  // ── 情绪相机（cameraOverride 驱动）──
  // shake = intense，slow-zoom = calm/static，pulse = dramatic
  const cameraOverride = directorState?.emotionEffect.cameraOverride ?? "static";
  const isShake = cameraOverride === "shake";
  const isPulse = cameraOverride === "pulse";
  const shakeAmt = isShake && directorState ? directorState.emotion * 10 : 0;
  const pulseAmt = isPulse && directorState ? Math.sin(frame * 0.05) * 0.02 : 0;
  const shakeX = shakeAmt * Math.cos(frame * 3.1);
  const shakeY = shakeAmt * Math.sin(frame * 2.7);

  // ── 导演强调行为 → 视觉动画映射──
  // state.emphasisPointWord（词索引驱动，精确到词）优先于 state.emphasis（时间区间）
  const ep = directorState?.emphasisPointWord ?? directorState?.emphasis;
  const emphasisZoom = ep
    ? ep.action === "zoom-in" ? 1.15
    : ep.action === "subtitle-pulse" ? 1.1
    : ep.action === "flash" ? 1.05
    : 1.0
    : 1.0;
  const emphasisBreathe =
    ep?.action === "slow-down" ? 0.4
    : ep?.action === "pause" ? 0.0
    : 1.0;

  // ── 合成最终镜头变换──
  const cameraTransform =
    `scale(${introZoom * camPush * breathe * emphasisZoom + pulseAmt}) ` +
    `translate(${shakeX}px, ${shakeY}px)`;

  // 背景色
  const bgColor = background ?? "#0A0E14";

  // ── 情绪色调覆盖（emotion overlay layer）──
  // colorOverlay 叠加在背景色上，intense=红，calm=蓝
  const emotionColorOverlay = directorState?.emotionEffect.colorOverlay ?? "rgba(0,0,0,0)";

  return (
    <AbsoluteFill
      style={{
        backgroundColor: bgColor,
        width,
        height,
        overflow: "hidden",
        opacity: fadeIn,
        fontFamily: FONT_FAMILY,
        transform: cameraTransform,
        transformOrigin: "center center",
      }}
    >
      {/* 情绪色调覆盖层（intense=红晕，calm=蓝调） */}
      <div
        style={{
          position: "absolute",
          inset: 0,
          background: emotionColorOverlay,
          zIndex: 0,
          pointerEvents: "none",
        }}
      />
      {/* v10.3: Shot 渲染层（镜头系统 + 切换连续性） */}
      {/* 渲染在 elements 下方（zIndex=-1），当前+下一 shot 同时渲染，transition 区间渐变 */}
      {(() => {
        const { current, next, currentTransform, nextTransform, nextShotTransform, nextEmotionTransform, currentOpacity, nextOpacity, isTransitioning } =
          useShotsAroundFrame(layout.shots, cameraOverride, transitionPlan, emotions);
        if (!current) return null;

        // 当前 shot
        const progress = Math.min((frame - current.start) / current.duration, 1);
        const { shotTransform, emotionTransform } = getShotTransform(current, progress, cameraOverride, frame);

        return (
          <>
            {/* 当前 shot（出画：pan延续 + cross-zoom 放大 + 淡出） */}
            <div
              style={{
                position: "absolute",
                top: 0,
                left: 0,
                width: "100%",
                height: "100%",
                overflow: "hidden",
                zIndex: -2,
                opacity: currentOpacity,
              }}
            >
              <Img
                src={current.src}
                style={{
                  position: "absolute",
                  top: 0,
                  left: 0,
                  width: "100%",
                  height: "100%",
                  objectFit: "cover",
                  transform: `${shotTransform} ${emotionTransform} ${currentTransform}`.trim(),
                  transformOrigin: "center center",
                }}
              />
            </div>
            {/* 下一 shot（入画：pan延续 + cross-zoom 缩小 + 淡入 + 完整camera pipeline） */}
            {isTransitioning && next && (
              <div
                style={{
                  position: "absolute",
                  top: 0,
                  left: 0,
                  width: "100%",
                  height: "100%",
                  overflow: "hidden",
                  zIndex: -1,
                  opacity: nextOpacity,
                }}
              >
                <Img
                  src={next.src}
                  style={{
                    position: "absolute",
                    top: 0,
                    left: 0,
                    width: "100%",
                    height: "100%",
                    objectFit: "cover",
                    transform: `${nextShotTransform} ${nextEmotionTransform} ${nextTransform}`.trim(),
                    transformOrigin: "center center",
                  }}
                />
              </div>
            )}
          </>
        );
      })()}
      {/* 渲染所有元素（排除进度条，由下面单独处理） */}
      {sortedElements
        .filter((el) => el.id !== "progress-bar-bg" && el.id !== "progress-bar")
        .map((el) => {
          switch (el.type) {
            case "text":
              return <TextLayer key={el.id} element={el} frame={frame} />;
            case "image":
              return <ImageLayerEl key={el.id} element={el} />;
            case "sticker":
              return <StickerLayer key={el.id} element={el} />;
            case "shape":
              return <ShapeLayer key={el.id} element={el} />;
            case "background":
              return <BackgroundLayer key={el.id} element={el} frame={frame} />;
            default:
              return null;
          }
        })}

      {/* 进度条（特殊处理：动态宽度） */}
      {(() => {
        const totalDuration = sortedElements.reduce(
          (max, el) => Math.max(max, el.start + el.duration),
          300
        );
        const progress = Math.min(frame / totalDuration, 1);
        const barW = width * progress;
        return (
          <>
            {/* 进度条背景 */}
            <div
              style={{
                position: "absolute",
                left: 0,
                top: height - 8,
                width: width,
                height: 8,
                backgroundColor: "rgba(255,255,255,0.1)",
                zIndex: 998,
              }}
            />
            {/* 进度条前景 */}
            <div
              style={{
                position: "absolute",
                left: 0,
                top: height - 8,
                width: barW,
                height: 8,
                backgroundColor: "#FF6B6B",
                zIndex: 999,
                boxShadow: "0 0 10px #FF6B6B80",
              }}
            />
          </>
        );
      })()}
    </AbsoluteFill>
  );
};
